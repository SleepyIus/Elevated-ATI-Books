# Elevated ATI Build Guide Current Overlay

Status: synchronized repository-safe recovery mirror for private master Build Guide Parts 49-180; the approved MN/MH/AMS template phase is complete, all eight Pediatric focused family reviews are accepted, and the final Pediatric cross-surface handoff is passed.
Mode: DESIGN-CANON / HOLD-CONTENT.
Scope: Pediatric Nursing Templates section-first design-canon review through A15, including the accepted shared typography, title-strip, spacing, corner, metadata, control, sidebar, comparison-guide, descendant-role, and `40px` true-endpoint contracts; content population remains paused.
Last continuity update: 2026-08-13.

This file does not approve ATI educational content population. It mirrors the current task and recovery state without private-source material. Private master Part 49 contains the complete authoritative canon, Parts 50-155 preserve the earlier recovery, automation, AI-specification, clipping, rollout, and section-first evidence summarized below, Parts 156-175 record the shared title-strip/spacing consolidation and focused A11/A13 descendant-role work, Parts 176-177 record the comparison endpoint and Pediatric `40px` true-endpoint corrections, Part 178 records focused user confirmation of A13 plus the passed all-family endpoint regression, Part 179 records focused A15 approval and completion of the eight-family visual-review sequence, and Part 180 records the passed final responsive, interaction/accessibility, print, and integrity handoff. This file must remain synchronized after every completed batch.

Current heartbeat: Part 180 supersedes the earlier final-handoff-pending wording. The user confirmed every focused family comparison in the A1-A15 sequence, and the final responsive, interaction/accessibility, print, payload, link, ID, count, storage, and endpoint gates now pass. Automated validation remains distinct from the user's focused visual approvals. The Pediatric design-polish phase is complete; content population remains paused.

## Pediatric Final Handoff Current Update

Part 180 records the final Pediatric handoff. Desktop `1440 x 1000`, tablet `768 x 1000`, and phone `390 x 844` passed without horizontal overflow; drawer, search, modal focus, Filled/Blank tabs, same-family navigation, Escape close/focus restoration, single-card print, family print, full-template print, 179 unique IDs, canonical `12/9/5/13/9/116/9/6` counts, 179 resolving chapter links, storage keys, payload parity, and the prior 1,074-state endpoint matrix all pass. No product or learner-facing file changed during the handoff. The only next project action is deciding whether to commit and push the completed Pediatric and accepted MN design-canon changes.

## Current Reading Rule

1. Follow the latest explicit user instruction first. Only the user may change the current project mode.
2. Use Part 49 and the newest later continuation in the private `Elevated_ATI_Build_Guide_v3.md` as the complete living master canon and migration reference.
3. Use this file as its repository-safe current-state and recovery mirror.
4. Use `MN Design Canon Checklist.md` as a synchronized implementation checklist and `AMS Prototype MN Canon Audit.md` as historical AMS audit evidence; neither may override private master Part 49.
5. Use the archived branch `codex-build-guide-archive-20260707` only as the immutable July 4 baseline; its older recommendations do not override this living overlay.
6. If any guide recommends content expansion while mode is `DESIGN-CANON / HOLD-CONTENT`, pause and report that content population is not active.

## Current Objective

Build the durable shell/foundation before further content work. The accepted design canon uses a clear reference hierarchy:

- MN is the primary structural, template-geometry, spacing, responsive, and mobile-layout reference.
- AMS is the polished visual reference for typography finish, controls, modal presentation, compactness, and general visual refinement.
- Preserve each book's accent color and subject identity. Use original ATI template layouts only for official structural labels and box relationships, never as permission to add content.
- When a difference is not already resolved by canon, stop for focused comparison rather than inventing a broad hybrid rule.
- Use Wide desktop overlay only when MN and Pediatric share one full available-width canvas, capped at `1440px`; use Split-pane only for responsive checks. An active overlay MUST mirror the MN scroll position immediately and MUST NOT be judged from a half-width responsive iframe.
- For same-role sidebar vertical parity, compare the rendered header, theme control, progress strip, search panel, search input, section label, and every template group at desktop, tablet, and phone widths. MN's accepted stack uses a `29px` theme control, `33.5px` search input, and `8px 0 24px` list padding; same-role geometry MUST remain within `1px`.

Completed automation objectives: private `Elevated_ATI_Build_Guide_v4_AI_Spec.md` is a self-contained, cold-start-validated AI execution specification with 1617 unique normative requirements, and the complete MN/MH/AMS template clipping matrix has passed final cross-book integrity. The user completed and approved the overall MN/MH/AMS template visual review on 2026-07-17. Those two automations remain paused. Pediatric Batches 0-10 remain valid structure, behavior, and clipping evidence, but their ready-for-review state is superseded as current status by Part 115 because user review confirmed typography and search-treatment mismatches.

## Active Pediatric Section-First Reaudit

The Pediatric visual-parity lane is reopened. Sections 0 through 5 are passed, and the user visually approved the focused A7 Medication review in Part 153 and A11 System Disorder review in Part 172. The active item remains Section 6 Shared Modal/Card-Top Anatomy only, and `ati-pediatric-templates-design-polish` remains paused pending explicit user authorization.

0. **PASSED:** establish an exact MN reference contract and reusable measurement harness before another Pediatric product edit.
1. **PASSED:** align the landing upper half: mobile/Hub controls, hero spacing, eyebrow, title, supporting copy, print control, and first divider.
2. **PASSED:** align the search/discovery divider, shell, label/icon, input, placeholder, focus state, dimensions, and responsive behavior while retaining the existing hidden result-count state.
3. **PASSED:** align all eight collapsed accordion rows: typography, direct-child order, visible A-page badges, print controls, counts, progress rails, borders, gaps, and wrapping.
4. **PASSED:** align the lower landing page: post-list spacing, reset treatment, footer/source line, links, terminal rhythm, and the shared split-pane scroll endpoint.
5. **PASSED:** Fixed sidebar and responsive drawer: typography, hierarchy, search, counts, controls, spacing, explicit scrollbar treatment, overlay, focus, inert behavior, and keyboard interaction. The user visually confirmed the scrollbar and drawer subgates; prior objective interaction evidence remains valid.
6. **ACTIVE:** Audit common modal/card-top anatomy: shell, title area, controls, metadata, Student Name line, source/open-chapter lines, navigation, and responsive stacking. The A1 Filled title/header and lower Worked Example control-layout subgates now match MN's shared geometry, print treatment, and typography weights; Pediatric retains its intentional blue active-state accent and content-driven wrapping.
7. Audit shared card frames, heading strips, boxes, padding, gaps, corners, connectors, official relationships, and Filled/Blank geometry before family exceptions.
8. Audit actual card content typography in every family, including descendant `p`, `li`, direct text, `strong`, phase labels, nested boxes, list spacing, colors, and wrapping.
9. Run the final desktop/tablet/phone and exact-breakpoint interaction, print, payload, comparison, and user-review handoff.

Every relevant section MUST compare computed font family, size, weight, line height, letter spacing, and color. Search MUST pass visual styling and focus/responsive geometry in addition to filtering behavior. Same-role geometry MUST be within 1px unless content wrapping or a documented subject-specific exception explains the difference. No section may pass on no-clipping evidence alone; style, geometry, responsive, behavior/accessibility, and visual gates are separate. Shared primitives are corrected before family-specific structures to avoid repeating one defect across eight families.

Section 0 evidence: the local contract maps 101 shared roles across nine sections and records ten typography plus 49 container/layout properties. Thirty-six MN/PD state captures at `1440 x 1000`, `768 x 1000`, and `390 x 844` produced two identical readings each, 24 screenshots, zero browser warning/error events, and zero unexpected missing selectors. Section 1 passes the measured `44px/1.05` title, `15px/25px` copy, `12px/1.6/600/.16em` eyebrow, print-control, divider, and narrow-gutter contract; the one-line Pediatric phone title versus MN's two-line title is documented wording-driven wrapping. Section 2 passes the visible panel/input/placeholder/focus contract at all three viewports; Pediatric retains its blue accent and existing hidden result-count behavior. Section 3 restores the visible A-page badge contrast, removes the duplicate row margin so every accordion gap is `14px`, and matches the MN print and `8px` progress-rail treatment across all eight rows.

## Section 6 Latest Update

Part 139 records the user's visual acceptance of the focused A1 shared-box frame correction: `Related Content`, `Underlying Principles`, and `Nursing Interventions` use MN's `14px` corners, `47px` title strip, `11px 16px` strip padding, `24px` heading line height, and existing `14px 16px 16px` body geometry. Wide A1 boxes keep MN's `280px` minimum; the established one-column narrow state is content-driven (`min-height:auto`). The approval is limited to that shared box-frame geometry and does not approve other A1 or Section 6 roles. The preserved card text alone determines differing final box heights. Scripts, 179 cards/unique IDs, exact family counts `12/9/5/13/9/116/9/6`, 179 links, and exact temporary payload parity pass at SHA-256 `e182a9da22f124c67ce788ce04ce9a6aa57ad78243e77c754815f55c017be902`. Section 6 remains active; A3 Diagnostic Procedure is the next focused user review. Automated QA is not user visual approval.

## Section 6 A3 Current Update

Part 140 records the focused A3 Diagnostic Procedure shared box-frame correction after user review identified the same mismatch in direct and nested subtitle boxes. Pediatric A3 now uses MN's existing direct `14px` frame with a `47px` title strip (`11px 16px`, `24px` line height), and nested `11px` frame with a content-driven height, square `40.59375px` title strip (`9px 14px`, `21.6px` line height), and `12px 14px 14px` body padding. At wide, `768px`, and `390px`, two stable readings per role show matching frame geometry except a harmless `0.008px` renderer rounding in nested title-strip height. All nine A3 cards passed Filled and Blank scans with no non-intentional box overflow; interaction, payload, script, counts, link, and diff checks pass at SHA-256 `4230be4a39c27c7fad0c911ba1597cc036c5c2428a1027e1932e45d1253a30c2`. A3 remains pending user visual approval; Section 6 remains active. Automated QA is not user approval.

## Section 6 A3 Spacing Current Update

Part 141 records the focused A3 correction requested by the user for insufficient space between the metadata box and first content box. Pediatric A3 now uses MN's existing `18px` metadata-to-grid spacing, replacing only its incorrect `12px` A3 override. Live before/after measurements were `12px` and `18px`; two post-repair readings `300ms` apart are `18px`, and all nine A3 cards in Filled and Blank modes retain the same gap. The only observed box overflow is the pre-existing `14px` Potential Complications relationship connector, which is deliberate. The refreshed local comparison loads the current Pediatric payload; A3 remains pending user visual approval and Section 6 remains active. Automated QA is not user approval.

## Section 6 A3 Corner Current Update

Part 142 records the focused A3 outer Considerations-frame correction requested by the user. Pediatric A3 now uses MN's existing `16px` outer Considerations radius, replacing only the incorrect `8px` A3 override; the established padding and inner `11px` subtitle-box corners remain unchanged. Stable pre-repair readings were `8px`; two post-repair readings `300ms` apart are `16px` with zero container overflow. All nine A3 cards in Filled and Blank modes retain that `16px` radius. The fresh product and temporary payload are byte-identical at SHA-256 `1f7033cecf6673c0a90d10757daa1af6222ce527685fb17bb51db685dcfbe9b7`; script, registry, comparison HTTP, and diff checks pass. A3 remains pending user visual approval and Section 6 remains active. Automated QA is not user approval.

## Section 6 A3 User Approval

Part 143 records the user's confirmation that the current focused A3 Diagnostic Procedure comparison appears correct. The approval covers the A3 direct/nested frame, metadata-to-grid spacing, and outer Considerations-corner work in Parts 140-142 only; it does not approve the remaining Section 6 roles or another template family. Section 6 remains active. Automated QA remains distinct from this user approval.

## MN A5 Health Promotion Current Update

Part 144 records the focused user-reported MN A5 Health Promotion parent-content inset correction. The prior `0px` top inset put the first line against the header divider; MN now uses `14px 16px` parent-content padding, preserving the existing bottom inset, header, parent frame, connector tree, child boxes, and learning content. Two stable post-repair readings measure a `14px` header-to-first-line gap, and all seven A5 cards in Filled and Blank modes retain the corrected padding with zero parent-container overflow. Part 145 records the user's focused visual acceptance; this MN-specific correction does not alter the active Pediatric Section 6 scope. Automated QA is not user approval.

## MN A5 Health Promotion User Approval

Part 145 records the user's confirmation that the focused MN A5 Health Promotion correction looks great. The approval covers the `14px` parent-content inset only and does not approve another MN family or alter the active Pediatric Section 6 scope. Automated QA remains distinct from this user approval.

## MN A7 Medication Current Update

Part 146 records the user-requested restoration of the official A7 `Category Class` metadata row. All 18 MN Medication cards now render the required row after the existing medication field and before `Review Module Chapter`; Magnesium Sulfate, Nifedipine, and Intrauterine Device received the previously missing empty row. The IUD's existing `Medication / Device` label remains unchanged. No category value was added or inferred: the restored rows use the established empty writing line. Four MN scripts parse; 197 cards and 197 unique IDs remain; the refreshed local comparison loads; and `git diff --check` passes. Automated QA is not user approval.

## MN A7 Purpose-Box Width Current Update

Part 151 records the user-selected Pediatric geometry for the two inner official A7 Purpose boxes. At the same `1014px` outer group width, MN's former inherited `14px` inset produced two `487px` boxes; the scoped Medication Purpose group now uses Pediatric's `10px` inset and retained `10px` gap, producing two `491px` boxes. All 18 MN Medication cards passed stable Filled and Blank rendered checks with `0px` box overflow. The normal A7 body order, special IUD form, labels, content, official metadata, registry, links, IDs, counts, storage, MN accent, and non-Medication groups remain unchanged. Automated QA is not user approval.

## MN A7 Purpose-Header Current Update

Part 152 corrects the separate inner-header mismatch the user identified after the outer-width fix. The two regular A7 Purpose headers now use Pediatric's exact `15px` DM Serif Display type, `10px 16px` padding, `17.25px` line height, and resulting `38.25px` rendered strip height; the `11px` inner frame remains unchanged and clips the square strip. All 17 regular A7 cards passed stable Filled and Blank rendered checks across 34 states with zero header or box overflow. The special IUD form has no Purpose group and remains unchanged. Automated QA is not user approval.

## MN A7 Medication Focused User Approval

Part 153 records the user's confirmation that the focused A7 Medication template now appears complete. Approval covers the A7-focused visual and structural work in Parts 146-152 only; it does not close the broader Pediatric Section 6 review or authorize work on another family. Automated QA remains distinct from this user approval.

## Pediatric A9 Metadata-to-Grid Current Update

Part 154 records the focused correction requested after the Pediatric A9 content boxes appeared too close to the metadata panel. MN retains an `18px` metadata-to-grid boundary; Pediatric's A9 override incorrectly retained `12px` while its internal direct-box gaps already matched MN at `14px`. Pediatric A9 now uses the same `18px` metadata boundary while preserving its existing `14px` grid gaps, six-box structure, nested Considerations boxes, official labels, links, content, responsive behavior, and blue accent. The rebuilt local payload is byte-identical to the product, the local comparison returns HTTP 200, and the active A9 card retains the `18px` boundary in both Filled and Blank modes with zero horizontal box overflow. This automated evidence does not replace user visual approval.

## Pediatric A9 Title-Strip Current Update

Part 155 records the focused correction requested after Pediatric A9 direct and nested box-title bars appeared shorter than their MN references. Direct A9 strips now explicitly use MN's `15px` / `24px` / `11px 16px` contract with a `47px` rendered band; a broad Pediatric card rule had otherwise imposed a compact `17.25px` line height. Nested strips already use the matching `13.5px` / `21.6px` / `9px 14px` contract with a `40.59375px` band. The existing A9 corner caps, six-box skeleton, blue accent, labels, content, and responsive behavior remain intact. Part 156 records the user's focused visual approval of A9; it does not approve another family or replace automated evidence for a different surface.

## Pediatric A1-A15 Shared Primary-Box Spacing Current Update

Part 158 records the user-requested consolidated repair for recurring Peds box spacing. Every Peds family selector now uses one shared `18px` metadata-to-first-primary-group boundary and the primary box relationships use a shared `14px` gap: A1, A3, A5, A7, A9, A11, A13, and A15. The approved G&D `20px` outer / `12px` internal rhythm and Medication's `10px` Purpose subgrid remain intentional exceptions. The current payload has one parsed inline script, 179 template IDs, exact `12/9/5/13/9/116/9/6` family counts, and byte-identical temporary parity at SHA-256 `c1be617e4ab8a2ed7f721cfeb4d623c764b80ac92cc1bb627dfe373aa1957e73`; the live A1 check reads `18px` / `14px` / `0px` horizontal overflow. Automated QA is not user approval.

## Pediatric A11 Primary-Body Typography Current Update

Part 159 records the user-reported A11 primary-body typography mismatch. The Pediatric A11 three top-row and Patient-Centered Care explanatory boxes now use MN's `16px` DM Sans at `1.6` line height; A11 Safety uses MN's `13px` text at `1.55` line height. Direct headings and the intentionally denser Assessment and Complications regions were already correct and remain unchanged. Product-to-temporary-payload parity, parsing, 179-card/ID and `12/9/5/13/9/116/9/6` count checks, local HTTP availability, and `git diff --check` pass. The existing in-app comparison tab holds a stale child-frame payload that this client cannot reload, so a fresh cache-busting local load at `http://127.0.0.1:8766/elevated-ati-template-compare-pd-mn.html?v=a11-typography` remains required before rendered after-state evidence or user approval is recorded. Automated QA is not user approval.

## Pediatric A1 Typography Recheck

Part 160 records the first user-requested staged A1-A9 typography recheck. A1 Basic Concept was opened in a fresh live MN-versus-Pediatric comparison in Filled and Blank modes. Two readings 350ms apart were stable. The modal eyebrow, display title, Worked Example label, section headings, and primary content compute identically: DM Sans/DM Serif Display, matching sizes, weights, line heights, and letter spacing; content is `16px/25.6px`, direct section headings are `15px/24px`, and display titles are `28px/30.8px`. A content-driven strong-weight difference in the sampled source text is not a base typography mismatch. No product or learner-facing content changed. Automated rendered QA is not user approval. The next staged audit is A3 Diagnostic Procedure; Part 159's A11 fresh-render recheck remains separate.

## Pediatric A3 Typography Recheck

Part 161 records the second staged A1-A9 typography recheck. A3 Diagnostic Procedure was opened in a current live MN-versus-Pediatric comparison in Filled and Blank modes. Two readings 350ms apart were stable. The modal eyebrow, display title, Worked Example label, direct and nested headings, and direct/nested primary copy compute identically: metadata `11px/17.6px`, display title `28px/30.8px`, direct headings `15px/24px`, nested headings `13.5px/21.6px`, and body content `16px/25.6px`; all roles retain the same DM Sans/DM Serif Display families, weights, and letter spacing. Content-driven strong emphasis remains outside the base typography contract. No product or learner-facing content changed. Automated rendered QA is not user approval. The next staged audit is A5 Growth and Development; Part 159's A11 fresh-render recheck remains separate.

## Pediatric A5 Typography Recheck

Part 162 records the third staged A1-A9 typography audit. A5 Growth and Development was opened in a current live MN-versus-Pediatric comparison in Filled and Blank modes; two readings `350ms` apart were stable. The modal eyebrow, display title, Worked Example label, primary body copy, and Health Promotion parent heading match. The direct A5 title strips do not yet match: both use `14px` DM Serif Display at weight `400`, but MN computes a `22.4px` line height and Pediatric computes `24px` in both modes. This is the active A5-only correction item; no product or learner-facing content changed during the audit. Automated rendered QA is not user approval. Part 159's A11 fresh-render recheck remains separate.

## Pediatric A7 Direct Header Current Update

Part 150 records the user-reported mismatch in Pediatric direct Medication body title strips. The prior compact Peds strip measured `38.25px` with `10px` vertical padding and `17.25px` line height; the shared MN direct-body strip is `47px` with `11px 16px` padding and `24px` line height. All 13 Pediatric A7 cards now apply that direct-header stack while retaining their existing `13px` header caps, Purpose group, inner official boxes, blue accent, and learner content. Stable desktop, tablet, and phone checks show zero header overflow; automated QA is not user approval.

## Pediatric A7 Typography Recheck

Part 164 records the staged A7 Medication typography recheck and the user's focused A5 visual acceptance. The A7-specific correction is limited to the four metadata labels: they MUST use the MN-computed DM Sans `10px / 700 / 16px / 1.4px` contract rather than a heavier inherited weight. Fresh current local source checks repeated identically after 350ms and found zero label overflow. All 13 Medication cards were visited in Filled and Blank modes with the corrected labels; source text, official box structure, IDs, counts, links, storage, order, and Pediatric accent remain unchanged. Automated A7 QA is not new user visual approval.

## Pediatric A9 Typography Recheck

Part 165 records the staged A9 Nursing Skill recheck. The A9-specific correction is limited to existing metadata labels: they MUST use the MN-computed DM Sans `10px / 700 / 16px / 1.4px` contract rather than the former heavier inherited weight. Fresh source-payload parity passed at SHA-256 `8c36744f93455b5a85070a94382572a908f9204d12d021c24c5e1912552b041d`; all nine cards in Filled and Blank modes at `1440 x 1000`, `768 x 1000`, and `390 x 844` had stable readings with zero measured overflow. Search filtering, same-family endpoint navigation, and the existing Print control remain functional. Automated A9 QA is not new user visual approval.

## Accepted Template Canon Snapshot

- Main template landing pages should use MN's spacing rhythm, typography scale, search panel treatment, progress rows, footer/reset treatment, and measured landmark alignment. At desktop and split-pane widths, each collapsed template accordion renders at 92px outer height with a 90px header, 18px 22px header padding, 14px flex gap, and an 8px progress track. Keep the header controls as direct flex children in this order: caret, title, A-page badge, row print button, studied count, progress track. This direct order preserves MN's badge and print-control placement across pane widths.
- Opened template modals should keep the larger AMS-style title treatment while using MN's readable body text, calmer controls, source-line structure, box spacing, and mobile stacking behavior.
- Every worked template card should render its empty Student Name value as the shared AMS/MH-style writing line: at least `120px` wide, `18px` high, and a `1px dashed` current-border-color bottom edge in both Filled and Blank modes. The line must remain inside its metadata value cell at desktop, split-pane/tablet, and phone widths.
- Across Peds A1-A15, each `dl.template-meta` must end with an `18px` boundary before its first primary content group and primary box grids must use a `14px` relationship gap. Preserve intentional local internals: A5 Growth and Development uses its `20px` outer / `12px` child rhythm, and A7 Medication Purpose uses its `10px` inset / `10px` inner gap. Do not use a shared primitive to flatten those accepted structures.
- For A1/A3/A5/A7/A9 typography, use MN A7 Medication's `Complications` box as the readable body-text canon across books: actual `p`, `li`, and direct body text in both main and nested boxes uses 16px DM Sans at 1.6 line-height with normal weight. Keep existing MN heading-strip typography unchanged. Validate rendered child text rather than only the parent `.content`, because child rules can silently restore smaller text.
- `Open chapter` links belong on their own line below the source chapter/title line.
- Internal source labels such as `ALS in book` should not appear in learner-facing source lines.
- A5 Growth and Development should preserve MN's outer section card, inner boxes, connector behavior, and progress bar. The Expected Growth and Development divider and child boxes stay inside the bordered, padded outer group; only the Health Promotion tree keeps a transparent outer wrapper for its connectors. Wide-grid siblings remain equal through normal row stretching, while each row is sized by its tallest real content; do not impose fixed `260px`/`300px` minimum heights. At split-pane and mobile widths, stacked A5 boxes use natural content height so sparse books do not retain large empty regions.
- A7 Medication should preserve the official MN/ATI medication skeleton, including Purpose of Medication, official left/right body columns, and the Nursing Interventions to Client Education connector. The Purpose group uses a `10px` inset and `10px` inner gap so its two equal official boxes share Pediatric's approved wider geometry at the same outer width. Its two regular inner Purpose headers use the Pediatric-approved `15px` type, `10px 16px` padding, `17.25px` line height, and `38.25px` rendered height inside their existing `11px` frames; do not let the generic compact header override replace this stack. Direct body title strips use the MN-rendered `47px` stack: `11px 16px` padding, `24px` line height, and existing `13px` top caps; do not let the generic compact `10px` header override shorten them.
- A7 Medication metadata labels MUST use DM Sans `10px`, `700` weight, `16px` line height, and `1.4px` letter spacing. The four labels remain part of the existing metadata structure; do not add, remove, rename, or reorder them to satisfy a typography check.
- A9 Nursing Skill should preserve the official six-box skeleton and keep source sections routed into the correct boxes instead of removing empty official slots. The two nested Considerations boxes use the same 16px DM Sans, 1.6 line-height body typography as the direct A9 boxes. A9 uses book-accent bullets, muted body text, and the book's solid primary text color for bold emphasis, including `Pre:`, `Intra:`, and `Post:`. Match MN's rounded A9 corner family exactly: 16px for the Considerations group, 14px for direct boxes with 13px header caps, and 11px for nested boxes with 10px header caps. At MN's wide and split-pane sizes, preserve the two-column procedure grid; collapse to one column only at the MN phone breakpoint of 680px. A9-specific grid-area rules must agree with the active column count so no implicit 2px column or oversized Considerations row can form.
- A11 System Disorder should preserve the official skeleton. On wide screens, Safety Considerations and Complications stay in one right rail with equal content-aware rows that grow to the larger content requirement; do not use shrinkable fractional rows or forced full-height children that can clip text. At narrow/tablet widths, explicitly override A11's higher-specificity desktop tracks so both the outer grid and filled top row use one column; never combine a one-column area map with two-column tracks. The Safety rail returns to natural vertical stacking, Assessment remains a 2-by-2 inner grid, and Patient-Centered Care uses the stacked five-box layout for readability.
- A13 Therapeutic Procedure follows the shared procedure-family sizing rules. Above its accepted 900px safeguard, paired rows stay equal but grow to the larger real content requirement; direct Considerations subboxes must not shrink or clip. At 900px and below, use one full-width MN-style vertical stack with natural-height Considerations boxes and hide the horizontal connector.
- A15 Concept Analysis preserves the simple official skeleton: two metadata rows (`Student Name` and `Concept Analysis`), Defining Characteristics spanning both wide columns, then paired Antecedents/Negative Consequences and Related Concepts/Exemplars. Use the shared 14px box gap and corners, 16px/1.6 body typography, natural content heights, and A15's own approximately 720px one-column breakpoint.
- Low-priority controls such as `Reset All Progress` should use MN's quiet text-button treatment: transparent background, inherited font, 11px size, and normal weight.

## Rollout Rules

- Do not roll out to all books in one run.
- Work one book template file at a time.
- Within each book, work one template family or one visual defect per batch.
- Preserve each book's accent color, subject identity, template counts, and existing educational content.
- Do not add templates, flashcards, prompts, cards, sections, enrichment, or ATI educational content while content population is paused.
- Use recovered screenshots only as QA/reference context, not public content.
- Before editing, state the exact file and layout area. Validate with static checks, script parsing, and visual comparison after editing.
- Never mark a visual task approved without user confirmation.
- Never stage, commit, push, merge, reset, revert, or delete without the user's explicit instruction.

## Documentation Continuity Protocol

- After every completed batch, update the private master Build Guide and this living overlay with the active book/template, completed work, files involved, validation, visual-approval status, unresolved issue, Git state, and exactly one next action.
- Check every current guide after each batch and update every affected guide so the current documents cannot contradict one another.
- Update `MN Design Canon Checklist.md` whenever an accepted canon rule is added, changed, clarified, or rejected.
- Update `AMS Prototype MN Canon Audit.md` whenever AMS audit status, comparison evidence, or rollout readiness changes.
- Keep the archived July 4 branch immutable as the recovery baseline.
- No migration-critical decision, pending approval, active task, or next action may exist only in chat history.
- Before ending a batch, confirm that a new task could resume safely from the private master, this overlay, the canon checklist, and the Git diff.

## Current Rollout Status

AMS and MN Templates have completed their major design-canon alignment. Their accepted comparison work established:

- main template landing-page alignment;
- source-line and modal header rhythm;
- quiet text/control token matching;
- A5 Growth and Development mobile/card behavior;
- A7 Medication structure and connector behavior;
- A9 Nursing Skill official-box preservation;
- A11 System Disorder Assessment and stacked Patient-Centered Care behavior;
- A13 procedure-family narrow/mobile behavior;
- A15 visual-token consistency.

MN has been changed only when explicitly approved during this prototype work, such as adding Previous/Next navigation to MN template modals, applying the stacked five-box Patient-Centered Care narrow layout, and adding the shared AMS/MH-style dashed Student Name writing line to every MN worked template card.

The MH Templates focused rollout is complete. The landing shell, sidebar controls, typography, accordion geometry, A-page badges, print controls, A3/A5/A7/A9 structures, and A9 corner/emphasis canon were aligned in small visual batches. MH A11 System Disorder now explicitly stacks the outer grid, filled top row, Safety rail, and five Patient-Centered Care boxes at narrow/tablet widths while preserving the 2-by-2 Assessment grid. MH A13 Therapeutic Procedure now replaces its higher-specificity desktop tracks with one full-width column and natural-height Considerations boxes at narrow/tablet widths. MH A15 Concept Analysis now uses MN's shared visual tokens, natural heights, official two-row metadata, and two-column-to-one-column responsive map. The final shell pass added MN's `max-width:1100px` main-workspace padding to MH and objectively validated the landing shell, sidebar, accordion, modal, navigation, count, clipping, search, and responsive behavior.

The MN A13 repair reported after that queue now passes objective clipping QA. Its content-aware procedure rows preserve equal left/right sizing while allowing the taller real content to determine row height. The user completed and approved the overall template visual review on 2026-07-17.

The Pediatric Nursing Templates family, structure, behavior, clipping, section-first visual parity, and final cross-surface handoff are passed. Parts 130-179 preserve the completed section and family review evidence; Part 180 closes Section 6 and supersedes every earlier Pediatric active-item statement. The Pediatric automation remains paused, and no Pharmacology batch is active.

## Current Recovery Snapshot

- Completed documentation files: private v4, private v3 continuity ledger, this overlay, the implementation checklist, and the AMS evidence ledger. No clipping matrix item remains active.
- Final-integrity baseline: the MN, MH, and AMS clipping matrix passed before the later user-approved MN Student Name writing-line alignment. The current MN payload now has SHA-256 `5d59aeaf4dc21236d989671a725a5803073c9a96e5e32f9842af79540ac0b31e`; four scripts, 197 cards, 197 unique IDs, family order, counts, and shell landmarks remain unchanged, and both comparison pages embed the exact current MN payload.
- Heartbeat status: `ATI Build Guide AI Spec Polish` (`ati-build-guide-ai-spec-polish`) and `ATI Template Clipping QA` (`ati-template-clipping-qa`) are paused and unchanged. `ATI Pediatric Templates Design Polish` (`ati-pediatric-templates-design-polish`) remains paused with its Part 115 section-first prompt and protected configuration unchanged; it must not resume without the user's explicit instruction.
- Pediatric Batch 0 baseline: repository and temporary PD payloads are byte-identical at SHA-256 `f6e66b9ee8f76f6c2ce0cbdb3687ed63ffea336a7c11b915d8336e3df370f1c5`; one inline script parses; 179 cards, 179 unique card IDs, exact family order and counts `12/9/5/13/9/116/9/6`, 179 resolving chapter links, unique static IDs, required shell landmarks, and storage keys `nur2460_theme`, `atipd_templates_progress`, and `atipd_last_template` passed.
- Pediatric Batch 0 rendered baseline: standalone desktop `1440 x 1000`, tablet `768 x 1000`, and phone `390 x 844` states retained all 179 cards, all eight groups, and `0px` horizontal page overflow. Search, A1 expansion, Filled/Blank, same-family Previous/Next, print-control presence, and modal close passed. Stable desktop and phone modal measurements had no controls outside the modal and `0px` horizontal page or modal overflow. Automated QA is not user visual approval.
- Pediatric comparison preview: `http://127.0.0.1:8766/elevated-ati-template-compare-pd-mn.html?v=final-pediatric-handoff-20260813` embeds current PD and MN payload copies and returns HTTP 200. Its Wide desktop overlay remains the full-width geometry tool; Split-pane remains the responsive comparison tool. The final current Pediatric payload SHA-256 is `860d781998acaeeeae150d4ed58e0ce84e23bfa68941d0055f3fa5e5294eae3a`.
- Pediatric Section 6 final update: the user-approved family sequence and Part 178 all-family endpoint matrix are complete. Part 180 adds the passed desktop/tablet/phone shell, drawer, search, modal semantics and focus, Filled/Blank, same-family navigation, single-card/family/full-template print, script, registry, link, storage, and payload-parity gates. Section 6 is closed. Automated QA remains distinct from user approval.
- Pediatric A1 family: objectively passed after one focused CSS correction. All 12 Basic Concept cards were measured in Filled and Blank modes at `1440 x 1000`, `768 x 1000`, and `390 x 844` before and after repair, for 72 stable post-repair states. The grid now uses the accepted `14px` gap and every measured `p`, `li`, `table`, `th`, and `td` computes at `16px/1.6`; all states retained exact metadata/box order, three wide columns or one narrow column, contained `120px` by `18px` dashed Student Name lines, intact Blank structure, and zero objective overflow. Current PD Templates SHA-256 is `e2cfb23e636265f28d01a1191f75870448ffd3d998589645ae7a9ea23ea557fd`.
- Pediatric A3 family: objectively passed after one cohesive CSS correction. The prior narrow state retained a higher-specificity desktop area map, producing implicit sliver tracks, rows up to `10,997.8px`, and up to `111px` of box/content overrun. All nine cards now pass Filled and Blank validation at `1440 x 1000`, `768 x 1000`, `390 x 844`, `861 x 1000`, and `860 x 1000`: 90 stable states with exact labels/relationships, `14px` gap, `16px/1.6` body typography, natural heights, `500px 500px` desktop tracks, exact 860px one-column transition, contained desktop connector, usable `120px` Blank boxes, intact Student Name lines, and zero objective overflow. The post-A3 PD Templates SHA-256 was `93170cc2db6587f3d9954a3b2e3657b5ff51ebd8040b98f3997322cfba941837`.
- Pediatric A5 family: objectively passed after one cohesive CSS correction. All five cards now preserve the bordered/padded Expected Growth and Development group and transparent Health Promotion connector tree; use `20px/12px` spacing, `16px/1.6` body typography, natural Filled heights, exact 4/2/1 tracks around 1100px and 600px, leak-free Blank mode, and `120px` writing areas. Seventy stable Filled/Blank states across desktop, tablet, phone, 1101px, 1100px, 601px, and 600px had exact labels/relationships, contained Student Name lines, zero answer leakage, and zero objective overflow. Current PD Templates SHA-256 is `77b8cb40a3fb917b592e2b9f950c6673c89dc223ffaa812bcc2f5b3a019906c6`.
- Pediatric A7 family: objectively passed after one cohesive CSS correction. All 13 cards retain the Purpose group, all nine official boxes, four metadata labels, and the Student Name line; use a `14px` body gap, `16px/1.6` body typography, content-driven Filled heights, permanent empty slots, `120px` Blank writing areas, the exact 680px stack transition, and a visible contained Nursing Interventions connector. One hundred thirty stable Filled/Blank states across desktop, tablet, phone, 681px, and 680px had zero answer leakage, zero content/page overflow, and zero objective clipping. Current PD Templates SHA-256 is `a2de1a39fc2c1d2c7ba2ba7435a7239286b53dec546c3c090a31234eae13dbad`.
- Pediatric A9 family: objectively passed after one cohesive CSS and renderer-guard correction. All nine cards retain the permanent six-region skeleton, three metadata rows, two nested Considerations boxes, `14px` relationship gap and connector, `16px/1.6` body typography, accepted `14px/16px/11px` corner hierarchy, natural Filled heights, and `120px` Blank writing areas. Ninety stable Filled/Blank states across desktop, tablet, phone, 681px, and 680px had exact two-column geometry through 681px, the ordered one-column stack at 680px, zero narrow overflow, and zero unexpected wide overflow. The post-A9 PD Templates SHA-256 was `453dfc05a412e5676b5856130dbb4884025a5b7fde5a88aa070630d40f40dc69`.
- Pediatric A11 family: objectively passed after one cohesive CSS and exact-routing correction. All 116 cards retain the permanent 14-region System Disorder skeleton, three metadata rows, dashed Student Name line, strict title-first routing, equal content-aware Safety/Complications rail, permanent empty boxes/groups, natural heights, explicit rail minimum sizing and wrapping, 2-by-2 Assessment grid, and stacked five-box Patient-Centered Care geometry at narrow widths. The final matrix covered 1,160 stable Filled/Blank states at desktop, tablet, phone, 901px, and 900px with zero instability, title/order failure, hidden official region, unequal wide rail, geometry failure, or objective clipping. Current PD Templates SHA-256 is `3647ee5d223bfebd865a6977b48bfc17d518b12c69fd8ab6f90e6dbb0cde8b87`.
- Pediatric A13 comparison endpoint correction: the temporary local comparison now synchronizes the active open-modal scrollers by normalized progress instead of raw pixels. This preserves legitimate content-driven modal lengths while aligning `0%` at both tops and `100%` at both bottoms. The reproduced MN/Pediatric ranges were `4774px` and `2089px`; both endpoints now synchronize exactly. Wide-overlay scrolling remains raw-coordinate based. No repository product or learner content changed, and A13 remains the only active visual-review item.
- Pediatric Batch 9 interaction-state gate: passed after one cohesive shared-control correction. All eight landing/sidebar family controls now expose resolving relationships and synchronized expanded state; landing rows are keyboard-operable; the responsive drawer synchronizes visible, programmatic, inert, and focus state; the modal moves, contains, and restores focus; runtime modal IDs remain unique and clones clear on close; Filled/Blank exposes synchronized tab/tabpanel semantics; and Blank persists through progress refresh plus same-family Previous/Next. Desktop, tablet, and phone shell/modal measurements were stable with `0px` page, modal-shell, modal-body, open-drawer, and header horizontal overflow. Current PD Templates SHA-256 is `ee4ba480e795b517ec1b60efcb1a1f9e27b1b121605af0562ac678859319f2a1`; automated QA is not user approval.
- Superseded Pediatric Batch 9 geometry evidence: the earlier `94.1953125px` header and child-order mismatch was repaired in Batch 9. Current desktop and tablet headers match MN at `89.59375px` header / `91.59375px` outer height with direct caret, title, A-page badge, print, studied count, and progress children.
- Approved comparison previews: `http://127.0.0.1:8765/elevated-ati-template-compare-mh-reference.html` and `http://127.0.0.1:8765/elevated-ati-template-compare-ams-mn.html`; both returned HTTP 200 and the user approved their current visual direction on 2026-07-17.
- User visual-review gate: **COMPLETE / APPROVED**. The user reviewed both comparison pages and accepted the current MN/MH/AMS template direction, including the MN Student Name writing line, on 2026-07-17. This subjective approval follows and remains distinct from automated QA.
- MN Student Name writing-line alignment: complete. One focused CSS selector now gives all 197 unchanged empty Student Name values the shared `120px` by `18px`, `1px dashed` line in Filled and Blank modes. Automated rendered measurements at `1440 x 1000`, `768 x 1000`, and `390 x 844` found the line fully contained with `0px` page and modal horizontal overflow. No learner-facing text, labels, source lines, counts, IDs, card order, or educational content changed.
- First automated batch status: complete. At a 1440px comparison viewport with approximately 720px panes, automated rendered QA found that Filled-mode Patient-Centered Care still inherited the desktop three-column tracks. The focused selector-specificity repair now gives all five boxes the full 548px content width; the 570px outer grid and top row are one column, Assessment remains 269px/269px, Safety stacks naturally, and no A11 box overflows.
- Second automated batch status: complete. At approximately 640px comparison panes, automated rendered QA found that MH A13 retained its desktop two-column area map beside a one-column narrow rule, collapsing one track to 2px and stretching the modal to 10,875px. The focused Therapeutic Procedure override now renders one 490px-wide track in the required six-region order, uses natural-height Considerations subboxes, has no box overflow, and reduces the modal to 1,733px. Scripts parse, the embedded comparison payload matches the current 71-card MH file, and all 10 A13 entries remain.
- Third automated batch status: complete. At approximately 1100px and 640px comparison panes, MH A15 now matches MN's 14px gap and corners, 16px/1.6 body typography, official two-row metadata, natural content heights, and two-column-to-one-column responsive behavior. All five narrow regions render at 490px with no box overflow. Scripts parse, the comparison payload matches, and all 71 cards, 71 unique IDs, and 6 A15 entries remain.
- Fourth automated batch status: complete. MH now mirrors MN's `max-width:1100px` `.template-main` padding of `40px 32px 80px`, removing the former 8px split-pane landmark offset. At approximately 1100px panes, the hero, title, body, divider, search panel, and first accordion share the same vertical landmarks and content left edge. Narrow panes preserve the accepted off-canvas sidebar and mobile padding behavior. Mobile modal, same-type Previous/Next, source link, print and mode controls, search synchronization, and count restoration were exercised without horizontal overflow. Static checks confirmed 71 cards, 71 unique IDs, eight groups, expected per-type counts, exact comparison payload parity, script parsing, HTTP 200, and a clean diff check.
- MN A13 clipping batch status: objectively passed with no additional template edit. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 22 cards passed in Filled and Blank modes, all 132 state measurements were stable on repeat, and the collapsed header passed at every width. No confirmed clipping remained. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 22 A13 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A1 clipping batch status: objectively passed after the focused Bishop Score repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 46 cards passed in Filled and Blank modes, all 276 state measurements were stable on repeat, and the collapsed header passed at every width. The Bishop Score table remained clear, with no additional confirmed defect. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 46 A1 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A3 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 15 cards passed in Filled and Blank modes, all 90 state measurements were stable on repeat, and the collapsed header passed at every width. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 15 A3 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A5 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 7 cards passed in Filled and Blank modes, all 42 state measurements were stable on repeat, and the collapsed header passed at every width. A5 outer groups, child boxes, and Health Promotion relationship geometry remained within their intended containers. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 7 A5 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A7 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 18 cards passed in Filled and Blank modes, all 108 state measurements were stable on repeat, and the collapsed header passed at every width. A7 Purpose, medication columns, direct and nested boxes, controls, and the Nursing Interventions-to-Client Education relationship stayed within their intended containers. A 7px list-row scroll delta at split-pane width was verified as visible list-marker geometry fully contained within its parent, not clipping. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 18 A7 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A9 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 15 cards passed in Filled and Blank modes, all 90 state measurements were stable on repeat, and the collapsed header passed at every width. The six official regions, seven measured direct/nested boxes, Considerations geometry, controls, and content remained within their intended containers. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 15 A9 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A11 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 65 cards passed in Filled and Blank modes, all 390 state measurements were stable on repeat, and the collapsed header passed at every width. The top row, Assessment, Safety, Complications, Patient-Centered Care, controls, and content remained within their intended containers. An initial bounds signal was rejected after confirming that `.sd-middle` intentionally uses `display: contents`; corrected measurements against `.sd-grid` were clear. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 65 A11 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A15 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 9 cards passed in Filled and Blank modes, all 54 state measurements were stable on repeat, and the collapsed header passed at every width. The five official boxes, two metadata rows, controls, and content remained within their intended containers, with no horizontal page overflow. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 9 A15 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A1 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 13 cards passed in Filled and Blank modes, all 78 state measurements were stable on repeat, and the collapsed header passed at every width. The three official Basic Concept boxes, three metadata rows, controls, and content remained within their intended containers, with no horizontal page overflow. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 13 A1 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A3 clipping batch status: objectively passed after two focused repairs. The first repair replaced the conflicting narrow area map and collapsed `2px 606px` tracks with one full-width column and natural-height boxes. The second repair follows MN relationship canon by hiding the horizontal Potential Complications connector only when MH A3 stacks at `max-width:860px`. All six cards passed in Filled and Blank modes at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 36 state measurements were stable on repeat, and the collapsed header passed at every width. Desktop retains the visible contained connector and `502px 502px` columns; tablet and phone retain full-width one-column boxes with no obsolete horizontal bridge. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A3 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A5 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all six cards passed in Filled and Blank modes, all 36 state measurements were stable on repeat, and the collapsed header passed at every width. Both official outer groups, all eight child boxes, three metadata rows, controls, direct content, and Health Promotion relationship geometry remained within their intended containers. Desktop preserved its contained four-column connector tree; tablet and phone used natural-height one-column stacks with the obsolete wide connector hidden. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A5 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A7 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 12 cards passed in Filled and Blank modes, all 72 state measurements were stable on repeat, and the collapsed header passed at every width. Both Purpose boxes, four official left-column boxes, three official right-column boxes, four metadata rows, controls, content, and the Nursing Interventions-to-Client Education connector remained within their intended containers. Desktop preserved the two-column medication body; tablet and phone used the intended one-column body while the connector stayed visible and contained. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 12 A7 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A9 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all six cards passed in Filled and Blank modes, all 36 state measurements were stable on repeat, and the collapsed header passed at every width. The six official regions, five direct boxes, two nested Considerations boxes, three metadata rows, controls, and content remained within their intended containers. Desktop retained its contained two-column connector geometry, split-pane/tablet retained two columns without clipping, and phone used one full-width column. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A9 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A11 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 12 cards passed in Filled and Blank modes, all 72 state measurements were stable on repeat, and the collapsed header passed at every width. The three-box top row, four-box Assessment group, Safety/Complications rail, five Patient-Centered Care boxes, three metadata rows, controls, and content remained within their intended containers. Desktop preserved the established wide geometry; split-pane/tablet and phone used one-column outer/top tracks with natural Safety stacking while retaining the 2-by-2 Assessment grid. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 12 A11 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A13 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 10 cards passed in Filled and Blank modes, all 60 state measurements were stable on repeat, and the collapsed header passed at every width. The six official regions, five direct boxes, Considerations group with two nested subboxes, three metadata rows, controls, content, and relationship connector remained within their intended containers. Desktop retained the contained `502px 502px` two-column grid and connector; split-pane/tablet and phone used natural-height one-column tracks with the obsolete horizontal bridge hidden. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 10 A13 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A15 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all six cards passed in Filled and Blank modes, all 36 opened-card states plus three collapsed-header states were stable on repeat. The five official boxes and two metadata rows remained within their intended containers. Desktop retained the `500px 500px` two-column grid; split-pane/tablet used one `618px` track and phone used one `354px` track. No page, box, control, or sibling overflow was confirmed. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A15 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A1 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 21 visible A1 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 126 opened-card states plus three collapsed-header states were stable on repeat. Every opened state retained three Basic Concept boxes and three metadata rows. Desktop retained three equal `328.664px` columns; split-pane/tablet used one `732px` track and phone used one `354px` track. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 21 A1 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A3 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all eight visible A3 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 48 opened-card states plus three collapsed-header states were stable on repeat. Every opened state retained seven direct/nested boxes and three metadata rows. Desktop retained the `500px 500px` procedure grid; split-pane/tablet used one `732px` track and phone used one `354px` track. The repeated 14px desktop `scrollWidth` delta on Potential Complications was verified as its intentional visible `::after` connector exactly spanning the 14px grid gap; the box content itself had zero overflow and the connector remained inside the grid, so it was not clipping. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all eight A3 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A5 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; its one visible prototype fixture passed in Filled and Blank modes, all six opened-card state measurements were stable on repeat, and the collapsed header passed at every width. Every opened state retained eight official boxes, two groups, the Health Promotion parent/connector relationship, and three metadata rows. Desktop retained four columns in both child rows, split-pane/tablet retained two columns, and phone used one full-width column with the connector hidden by the established mobile rule. The visible desktop/tablet connector overflow was verified as intentional stems that remain inside the Health Promotion group and do not clip content or sibling boxes. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, the one A5 prototype fixture remains, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A7 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 16 visible A7 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 96 opened-card states plus three collapsed-header states were stable on repeat. Every opened state retained nine official boxes, including both Purpose boxes, four left-column boxes, three right-column boxes, and four metadata rows. Desktop retained the two-column medication body; split-pane/tablet and phone used full-width stacked medication columns. The repeated 24px vertical delta on Nursing Interventions was verified as its intentional visible connector exactly crossing the gap to Client Education while remaining inside the right medication column; neither box's content overflowed. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 16 A7 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A9 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all four visible A9 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 24 opened-card state configurations plus three collapsed-header states were stable on repeat. Every opened state retained seven official direct/nested boxes, the Considerations parent, and three metadata rows. Desktop retained the `500px 500px` two-column procedure grid; split-pane/tablet and phone used full-width one-column grids in the accepted narrow order. The repeated 14px desktop delta on Potential Complications was verified as its intentional visible connector spanning the procedure-grid gap toward Nursing Interventions while remaining inside the grid; the connector is hidden by the established narrow rule. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all four A9 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A11 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 16 visible A11 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 96 opened-card state configurations plus three collapsed-header states were stable on repeat. Every opened state retained the three-box top row, four-box Assessment group, Safety Considerations plus Complications rail, five-box Patient-Centered Care group, and three metadata rows. Desktop retained the two-column outer grid, three-column top row, two-column Assessment, subgrid Safety rail, and three-column Patient-Centered Care geometry. Split-pane/tablet and phone used one-column outer/top tracks, retained two-column Assessment, stacked Safety/Complications naturally, and adapted Patient-Centered Care between one and two contained tracks according to the existing visible-content class. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 16 A11 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A13 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 17 visible A13 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 102 opened-card state configurations plus three collapsed-header states were stable on repeat. Every opened state retained seven official direct/nested boxes, the Considerations parent, and three metadata rows. Desktop retained the `500px 500px` two-column procedure grid; split-pane/tablet and phone used full-width one-column grids in the accepted narrow order. The repeated 14px desktop delta on Potential Complications was verified as its intentional visible connector spanning the procedure-grid gap toward Nursing Interventions while remaining inside the grid; the connector is hidden by the established narrow rule. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 17 A13 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A15 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; its one visible prototype fixture passed in Filled and Blank modes, all six opened-card state configurations were stable on repeat, and the collapsed header passed at every width. Every opened state retained five official Concept Analysis boxes and the three current rendered metadata rows. Desktop retained the `500px 500px` area map, split-pane/tablet retained two `359px` columns, and phone used one full-width `354px` column. Paired rows remained equal at wide widths, all boxes used natural contained heights, and no page, control, content, or sibling overflow was confirmed. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, the one A15 prototype fixture remains, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. This clipping-only pass did not change source-line metadata or establish a new metadata-row canon decision. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN shell clipping batch status: objectively passed with no template repair. The exact current standalone payload, local preview, and rebuilt temporary comparison matched with SHA-256 `ca58dc999c302ed8e20969d56bfb690bcad1a8080699056752becc05973b5900`; four scripts parsed, 197 cards and 197 unique IDs remained, eight family accordions stayed in canonical order, and 11 required shell landmarks remained. At `1440 x 1000`, `820 x 1000`, and `390 x 844`, all eight collapsed headers, landing controls, fixed/sidebar-drawer states, and one representative Basic Concept modal chrome state were stable on repeat. Page and modal-body horizontal overflow remained `0px`. The responsive drawer remained fully in view with `aria-expanded="true"`; intentional closed off-canvas and vertical modal-body scrolling were excluded. The preview was restored to a clean default landing state. Automated QA is not user visual approval.
- MH shell clipping batch status: objectively passed with no template repair. The current repository and standalone preview matched at SHA-256 `21fb16085668db5fa8d432e2bd8a20d149db947b2650c4d3ae53465f114c9d27`; the comparison embedded the exact payload; the single script parsed; and 71 cards, 71 unique IDs, eight groups, and counts `13/6/6/12/6/12/10/6` remained. At `1440 x 1000`, `820 x 1000`, and `390 x 844`, eight landing/modal/sidebar states were identical on repeat at least 300ms apart. Page, collapsed-header, open-sidebar, modal-control, and modal-body horizontal overflow remained `0px`; all eight collapsed headers and all eight open-drawer sidebar rows passed. Tablet/phone modal-body vertical scrolling was intentional, closed off-canvas state was excluded, and visible-overflow heading line-box deltas had no truncated edge. Browser console errors were zero. No screenshot or product repair was permitted. Automated QA is not user visual approval.
- Availability note: the user also observed one selected-model capacity warning. It may skip or delay an automation run but does not change project files; switch models only if it persists.
- Build Guide polishing Batches 1-16 are passed. Private v4 contains the complete AI execution specification and begins with the exact marker `ELEVATED_ATI_AI_SPEC_STATUS: COMPLETE_AND_COLD_START_VALIDATED`. Post-completion canon synchronization now reports 1617 unique requirements, complete section/traceability/protocol coverage, and matching cold-start artifacts. Automated validation is not user approval.
- Hub evidence recorded in Batch 5: all five current Hub shells have four ordered counters, three Primary tools, the manifest unit count, eight Template type rows, unique IDs, and parseable scripts. AMS retains the measured `84/76` Templates and `208/0` Starter Cards payload/display mismatches. PD has eight and Pharm has seven Hub Template-type hash targets that do not resolve as literal or script-handled family anchors in their current Templates files. These are documented `OBSERVED_MISMATCH` facts only; no Hub or Templates repair is authorized during Build Guide polishing.
- Book evidence recorded in Batch 6: current chapter/planner counts are AMS `96/96`, MH `33/33`, MN `27/27`, PD `44/44`, and Pharm `49/49`; all five files retain unique static IDs, required modal landmarks, four parseable inline scripts, intentional modal-body scrolling, and phone modal rules. MN lacks page-load/hashchange chapter routing, PD `ch35` and `ch37` lack Template bridges, and Pharm has TL;DR cards in 12 of 49 chapters and Template bridges in none. These are documented `OBSERVED_MISMATCH` facts only; no Book repair is authorized during Build Guide polishing.
- Templates evidence recorded in Batch 7: all five current Templates files retain the fixed eight-family A1-A15 registry, unique card/static IDs, parseable scripts, resolving local paths, Filled/Blank state, search, progress, same-family modal navigation, print, and modal controls. Family counts are AMS `21/8/1/16/4/16/17/1`, MH `13/6/6/12/6/12/10/6`, MN `46/15/7/18/15/65/22/9`, PD `12/9/5/13/9/116/9/6`, and Pharm `1/3/0/141/18/35/9/42`. AMS retains 76 learner/source-linked cards plus eight hidden prototype fixtures, Pharm A5 retains its zero-card placeholder, and AMS A15 currently renders three metadata rows against the accepted two-row canon. These are evidence or `OBSERVED_MISMATCH` facts only; no Templates repair is authorized during Build Guide polishing.
- Flashcards evidence recorded in Batch 8: current starter-card payloads are AMS/MH/MN/PD/Pharm `208/593/492/747/702`; all card IDs are unique, scripts parse, and every current source anchor resolves to the same-subject Book. AMS remains the older flat shell and displays seven types while its cards use five distinct values. The four mature shells split `easeFactor`/`repetitions` initialization from `ef`/`reps` rating aliases, and their Browse path can discard a selected non-due card. These remain `OBSERVED_MISMATCH` facts only; no Flashcards repair or migration is authorized during Build Guide polishing. Batch 8 verified 196 sequential `FC` requirements and 843 total unique v4 declaration IDs; the earlier 655 Batch 7 total was corrected to 646 without changing a requirement.
- Placeholder/new-husk protocol recorded in Batch 9: existing subjects must be measured and preserved rather than replaced; new subjects require explicit identity, edition, accent, four paths, isolated storage, and collision-free plans. Donor payload and identity must be removed outside the repository before a destination write. Placeholder construction order is Book, Templates, Flashcards, then Hub, one separately authorized surface per batch. Exact empty states and readiness flags keep content population paused. Batch 9 verified 120 sequential `HUSK` requirements and 964 total unique v4 declaration IDs; no repository husk or product surface was created or changed.
- Empty-box/content-population protocol recorded in Batch 10: only a current explicit user instruction can activate one bounded content batch for one subject and one surface. A private provenance map must cover 100% of the focused learner-facing diff with exact same-subject source locations and destination routes; inference, model memory, web material, donor content, and broad topic similarity cannot close a source gap. Required official boxes remain visible and machine-flagged when unsupported instead of being deleted or filled with stubs. Default caps are two Book chapters, three Template cards, or twelve Flashcards, with Hub count/status synchronization handled as a separate surface batch. Authorization expires when the batch closes, blocks, or reaches its cap, returning the project to HOLD-CONTENT. Batch 10 verified 135 sequential `CONTENT` requirements and 1100 total unique v4 declaration IDs; no ATI source, repository product surface, learner-facing content, count, link, card order, or manifest changed.
- Deterministic build pipeline recorded in Batch 11: every work order selects one focused-repair, placeholder-suite, source-authorized-content, or documentation lane and owns one subject, one surface, one primary file, and one bounded objective. New/recreated placeholder suites proceed as validated manifest, Book, Templates, Flashcards, then Hub; existing repairs begin directly with the named surface. The fixed phase order is authority/mode, work order, preflight, baseline, temporary preparation, implementation, static validation, rendered/behavior validation, focused diff, continuity, approval gate, and handoff. Product and documentation ownership are explicit, objective QA remains distinct from user approval, and no passed batch authorizes Git publication or another lane. Batch 11 verified 140 sequential `BUILD` requirements and 1241 total unique v4 declaration IDs; no product surface, learner content, source artifact, automation configuration, or AMS audit changed.
- QA specification recorded in Batch 12: every batch uses a requirement-backed matrix whose required rows must all be `PASS` or justified `NOT_APPLICABLE`, with zero `FAIL` or `BLOCKED` rows. Static QA uses structured parsing and complete scripts/targets; payload QA uses complete sets and independently measured counts; navigation requires zero unresolved live or unauthorized cross-subject targets; storage uses an isolated context; rendered QA uses `1440 x 1000`, `768 x 1000`, and `390 x 844` plus exact breakpoints; clipping requires more than 1px objective overflow and two identical readings at least 250ms apart. Screenshots remain supplemental under `/private/tmp`, and focused success never implies full conformance or user approval. Batch 12 verified 160 sequential `QA` requirements and 1402 total unique v4 declaration IDs; no product surface, source artifact, automation configuration, or AMS audit changed.
- Recovery/operations specification recorded in Batch 13: each active batch has one durable work order containing authority, mode/lane/scope, exact file/targets, editable/supporting/forbidden files, manifest/payload baseline, QA/approval gates, Git state, automation preservation, status, and one next action. Recovery uses current files/configurations only, resumes from the first unpassed phase, classifies blockers explicitly, keeps v3 append-only and v4 IDs stable, and treats commit and push as separately authorized operations. The final automation transition remains reserved for Batch 16 and may change only status fields after exact configuration capture/verification. Batch 13 verified 140 sequential `RECOVERY` requirements and 1543 total unique v4 declaration IDs; no product surface, source artifact, Git state, automation configuration, or AMS audit changed.
- Cold-start validation recorded in Batch 15: v4 alone produced a temporary test-only manifest, Book-to-Templates-to-Flashcards-to-Hub build plan, and validation report under `/private/tmp`. The initial synthetic code was rejected on a real repository identity collision; the accepted `V4X` fixture had zero path, identity, slug, course-label, namespace, or key matches. The final matrix reported 14 PASS, 0 FAIL, and 0 BLOCKED; all learner payloads remained empty, no product file was created or edited, and automated validation did not grant user approval or real-subject authority. Batch 15 verified 20 sequential `COLD` requirements and 1581 total unique v4 declaration IDs.
- Clipping matrix status: complete. All 24 MN, MH, and AMS template-family items, the MN/MH/AMS shells, and the final cross-book integrity gate are objectively passed and must not be repeated without a concrete regression.
- `ati-template-clipping-qa` is configuration-identical and PAUSED after automated QA and user visual review both completed.
- Pediatric Batch 7 A13 status: objectively passed after one cohesive procedure-grid correction. All nine cards retained exact metadata, official box relationships, labels, learner payload, links, IDs, and order; now use the shared `14px` relationship gap/connector, natural Filled heights, permanent official slots, `120px` Blank writing areas, and an exact 901/900 two-column-to-one-column transition. Ninety stable Filled/Blank states across desktop, tablet, phone, 901px, and 900px had zero objective clipping after repair. Current PD Templates SHA-256 is `58646dec8910cf1cad12a4bab50bb159b466fdd4d514dd37b804105037513998`; automated QA is not user approval.
- Pediatric Batch 8 A15 status: objectively passed after one cohesive metadata, spacing, typography, natural-height, corner, and responsive correction. All six cards preserve the exact two-row metadata and five official boxes; use `14px` gaps/corners, DM Sans `16px/1.6` body text, natural Filled heights, `120px` Blank writing areas, equal paired wide rows, and the exact 721/720 transition. Sixty stable Filled/Blank states across desktop, tablet, phone, 721px, and 720px had zero objective clipping after repair. Current PD Templates SHA-256 is `f7dd6120ade44347d7c13f935032bf126aec1e6253885fa1e1c60650fcbe724e`; automated QA is not user approval.
- Pediatric Batch 9 status: objectively passed after the interaction-state correction in Part 111 and the focused landing-header correction in Part 112. All eight landing rows now use direct caret, title, A-page badge, print, studied count, and progress children; desktop and tablet match MN at `89.59375px` header / `91.59375px` outer height; adaptive phone rows remain contained. Accordion, drawer, search, modal, mode, navigation, focus, progress, print, runtime-ID, and desktop/tablet/phone overflow gates pass. Current PD Templates SHA-256 is `72a1f358462d5098eaeb1d6dd2c0a963c2dd1f2f7f40e5784804347813f2a96d`; automated QA is not user approval.
- Pediatric Batch 10 completion status: `PD_TEMPLATE_DESIGN_STATUS: READY_FOR_USER_VISUAL_REVIEW`. The current product hash remained unchanged while final static parsing, 179-card/ID/count/order/link/storage checks, exact temporary payload parity, all eight family modal types in Filled/Blank mode, stable desktop/tablet/phone shell measurements, connector classification, drawer/search/navigation/focus behavior, empty browser logs, HTTP 200 comparison availability, and guide consistency passed. Automated QA is not user visual approval.
- Pediatric user-review Back-to-Hub correction: the narrow landing shell now exposes a visible Pediatric Hub link on the left and the responsive menu on the right, matching MN's accepted navigation pattern while retaining Pediatric's 860px off-canvas breakpoint and its own Hub destination. Stable desktop/tablet/phone measurements found no overlap or horizontal overflow; scripts, 179 cards/IDs, counts/order, chapter links, landmarks, storage keys, and comparison parity remain intact. Current PD Templates SHA-256 is `5bab1082071db7390a55da58f88333018a778289f7872e1d6d42d2edc4afbbff`; automated QA is not user approval.
- Pediatric section-first Section 0 status: passed without a product edit. The nine-section harness maps 101 roles and measures typography, containers, geometry, responsive states, search focus, sidebar/drawer, and common modal/card anatomy. All 36 state captures repeated identically, 24 screenshots remain outside the repository, browser warning/error logs were empty, and the final selector contract has zero unexpected gaps.
- Pediatric section-first Section 1 status: passed after one compact landing-only CSS correction. Stable desktop, tablet, and phone captures show the shared upper-half type/geometry contract within 1px; the phone title-height delta is the documented one-line Pediatric versus two-line MN wording wrap. One inline script, 179 cards, 179 unique IDs, exact `12/9/5/13/9/116/9/6` counts, and storage keys pass. Automated QA is not user visual approval.
- Pediatric section-first Section 2 status: passed after one search-only CSS correction. The visible filter panel, label, input, placeholder, and focus geometry match MN at desktop, tablet, and phone; the blue Pediatric focus ring is the accepted subject-accent difference. The pre-existing hidden result-count behavior was preserved, and no learner-facing search behavior or payload changed.
- Pediatric section-first Section 3 status: passed after one shared accordion correction. All eight rows match the measured MN title/badge/print/progress typography and geometry, the A-page boxes remain visibly distinct from their headers, every inter-row gap is `14px`, every inner progress rail is `8px`, and all desktop/tablet/phone readings repeated identically without horizontal overflow. Section 4 was the next item at that record.
- Pediatric user-review accordion entrance correction: the user-visible Pediatric spacing concern was traced to the search-to-first-row boundary, not the seven already-matched `14px` inter-row gaps. MN measured `12px`; Pediatric measured `18px` because its existing hidden result count removed MN's `-6px` layout contribution. Pediatric now preserves the hidden count and applies the same `-6px` compensation only while it is hidden. Both panes measure eight `92px` rows, seven `14px` gaps, and a `12px` entrance boundary; 179 cards and 179 unique IDs remain. Current PD SHA-256 is superseded by the Section 4 record below. Section 4 was the next item at that record.
- Pediatric section-first Section 4 status: passed after the user-requested lower-landing correction. The reset now matches MN's unboxed `11px` treatment, and the existing Pediatric footer uses MN's `60px` lead-in, `30px` padding, one-pixel divider, `13px` type, and `99px` footer footprint. The initial Section 4 hash is superseded by the footer-text correction below.
- Pediatric footer-text and endpoint correction: user review identified the missing two-line footer roles, then an `11px` shorter Pediatric split-pane scroll range. Pediatric now renders the companion line and the Pediatric-specific source line using existing subject, edition, page-range, and Hub metadata; the former standalone `Open PD Book` footer link is replaced by the MN-equivalent two-line footer role set. Its established `900px` responsive lower padding is now `71px`, giving both panes a `1166px` maximum range while retaining the `99px` footer footprint and zero footer overflow. Current PD SHA-256 is `d85b9794218a86d9ebaa530353eacbe0808953cb8514f9b6c0520ecdd0f2491c`. Its former Section 5 active-item statement is superseded by Part 130; Section 6 is current.
- Pediatric wide-overlay harness correction: the former Overlay control was a left split-pane overlay and therefore incorrectly evaluated responsive geometry as desktop. The local helper now provides a named Wide desktop overlay at one shared canvas (up to `1440px`), keeps Split-pane for responsive checks, and instant-mirrors MN scrolling to Pediatric. The related desktop terminal padding is `91px`; the established narrow terminal padding remains `71px`. The current wide rendered canvas has equal `1117px` frames, equal `1105px` maximum ranges, and exact `520px` scroll alignment. Current PD SHA-256 is `9b157a0f0a10df9eee90968a23c1e4a9033847b0b037c2875c9395edb0b06400`. Its former Section 5 active-item statement is superseded by Part 130; Section 6 is current.
- Pediatric sidebar vertical-stack correction: user review exposed a stable `2.60px` oversized Pediatric theme control and `3.70px` oversized search input, which accumulated to a `6.29px` sidebar-row offset. Pediatric now uses MN's normal line-height for both controls and its `8px 0 24px` list padding. Exact stable desktop, `768px`, and `390px` measurements match MN for the header, theme control, progress strip, search panel/input, section label, and all eight group rows; its recorded hash is superseded by the legend correction below. Section 5 remains active for drawer overlay, focus, inert-state, and interaction behavior.
- Pediatric sidebar legend correction: the `Reviewed` and `Checked` dots, baselines, and gaps were already aligned, but Pediatric's `600` legend weight widened the first label by `1.17px` and shifted the second role beyond the one-pixel gate. Pediatric now uses MN's `500` weight; both labels, dots, baselines, widths, and positions match exactly. Current PD SHA-256 is `8d074389f0ac5b3a485a8da9e30bfeb4f6aafe4f8adb3f08227e0675f55c3fe7`. Section 5 remains active for drawer overlay, focus, inert-state, and interaction behavior.
- Pediatric A7 Medication corner-frame restoration: the user identified that Pediatric's current Purpose group and Medication body boxes were flatter than MN. The narrowly scoped existing A7 modal overrides now use MN's exact hierarchy: `16px` Purpose outer frame, `11px` inner Purpose-box frames with square headers clipped inside them, and `14px` direct medication-body frames with `13px` header caps. Live `300ms` repeated readings match MN, retain zero new horizontal overflow, and preserve the existing shared `24px` Nursing Interventions vertical delta. One script parses; the 179-card/179-unique-ID registry and `12/9/5/13/9/116/9/6` family counts remain intact; product/local payload parity, local HTTP 200, and `git diff --check` pass. This is automated validation, not user approval. The three protected automations remain paused and unchanged.
- Pediatric A7 metadata-to-Purpose spacing restoration: user review found the direct A7 metadata card's lower margin at `14px` while MN measures `18px`. The scoped Pediatric A7 metadata override now uses MN's `18px` margin; two readings `300ms` apart match exactly, retain the corrected corner stack, and have `0px` horizontal card overflow. Script, registry, local payload parity, HTTP, and diff checks remain passed. This is automated validation, not user approval. The three protected automations remain paused and unchanged.
- MN A7 Medication body-order alignment to Pediatric: the user selected Pediatric's regular Medication body arrangement as the shared standard. Magnesium Sulfate and Nifedipine alone had retained Evaluation as a trailing full-width item; each existing intact Evaluation container now follows Interactions in its existing left column. All 17 regular MN Medication cards now use the exact standard left sequence `Complications`, `Contraindications/Precautions`, `Interactions`, `Evaluation of Medication Effectiveness` and right sequence `Medication Administration`, `Nursing Interventions`, `Client Education`, with no trailing direct body box. The special Intrauterine Device `Medication / Device` structure remains unchanged. Four MN scripts, 197 cards/unique IDs, family counts `46/15/7/18/15/65/22/9`, temporary payload parity, local HTTP 200, and `git diff --check` pass. This is automated validation, not user approval. The three protected automations remain paused and unchanged.
- Pediatric shared direct-title-strip consolidation: the user identified recurring narrow primary box headers and selected a consolidated repair. Primary direct template boxes use MN's `47px` title-band contract (`11px` vertical padding, `24px` line height, and the existing one-pixel divider) across A1/A3/A7-body/A9/A11/A13/A15. A5's Health Promotion parent uses that same `47px` band, but its eight direct Expected Growth and Development group headings are an accepted MN exception at `14px / 22.4px / 45.3984375px`, also with `11px` vertical padding. Nested headers and the deliberately compact A7 Medication Purpose headers remain separate. The current 179-card/179-unique-ID product and rebuilt temporary payload match at SHA-256 `fd0f2b8f01087805cd2087ede220ff53ceef7ddc1cc4225749374513efcd40d3`; one script parses, the family counts remain `12/9/5/13/9/116/9/6`, the comparison returns HTTP 200, and `git diff --check` passes. Automated validation is not user approval.
- Pediatric A5 direct-heading line-height alignment: a staged typography check found a stable `1.6px` direct-heading line-height mismatch. The existing A5 direct-group selector now uses MN's `22.4px` line height while retaining the approved `11px` vertical padding; the Health Promotion parent retains its already-matching `24px` line height. Fresh current-payload QA checked all five A5 cards in Filled and Blank modes: all 80 direct headings measured `14px / 22.4px / 45.3984375px` with zero measured overflow. Automated validation is not user approval.
- Pediatric shared primary-box spacing consolidation: the user identified recurring inconsistent space between the metadata panel and primary boxes, and requested one repair through A15. A1/A3/A5/A7/A9/A11/A13/A15 now share `--tpl-meta-to-primary-gap:18px` and `--tpl-primary-box-gap:14px`; direct layout grids and family metadata selectors use those shared values. A5's approved `20px` outer / `12px` inner group rhythm and A7 Medication Purpose's `10px` inset / `10px` inner gap remain explicit exceptions. The refreshed product and temporary payload are byte-identical at SHA-256 `c1be617e4ab8a2ed7f721cfeb4d623c764b80ac92cc1bb627dfe373aa1957e73`; the live A1 modal produces two identical `18px` boundary / `14px` gap / `0px` page-overflow readings. Automated validation is not user approval.
- Current tracked modifications: `Elevated ATI MN Templates.html`, `Elevated ATI PD Templates.html`, `Build Guide Current Overlay.md`, and `MN Design Canon Checklist.md` for the focused MN frame/metadata/body-order/Purpose-header restorations, Pediatric parity corrections, and current section-first continuity.
- Current branch: `main`, synchronized with `origin/main` after pushed commits `9de1043 Complete template clipping QA and alignment` and `33d0968 Fix AMS template hero button alignment`.
- Known local untracked files: `.DS_Store`, `Teleprompter.html`, and `recovered-codex-screenshots/`; report them only under GitHub Desktop status.

## Pediatric A13 Descendant-Typography Current Update

Part 175 corrects the body-type inheritance gap exposed after Part 174. Every direct A13 `p`, `li`, list, and table descendant now inherits the direct DM Sans `16px / 25.6px / 400` role; every nested Considerations descendant inherits `13px / 19.5px / 400`. Direct title strips remain `15px / 24px` with `11px 16px` padding; nested strips remain `13.5px / 21.6px` with `9px 14px` padding. All nine cards pass Filled and Blank descendant audits with zero role deviations and zero measured overflow at desktop, tablet, and phone. The current product and temporary comparison payload are byte-identical at SHA-256 `ab2b2c1b8c7fa70245a8cc64c7fddd8c66b68f3ae1833653e17c6dc4072c3dde`. This objective pass is not user visual approval.

## Pediatric A13 True-Endpoint Current Update

Part 177 supersedes the implication that comparison scroll synchronization alone resolved the user's A13 bottom mismatch. Fresh-payload evidence found an actual product delta: MN retained `40px` modal-body bottom padding while Pediatric's responsive rule used `36px`. Pediatric now retains `40px` at that breakpoint. MN `Vasectomy` and Pediatric `Cast Care` endpoint gaps measure `40.0625px` and `40.34375px`, a `0.28125px` browser-rounding difference below the `1px` gate. All nine Pediatric A13 cards pass Filled and Blank endpoint checks at desktop, tablet, and phone: 54 states, stable repeated readings, `39.203125-40.84375px` range, zero failures, and zero browser errors. The current product and temporary payload are byte-identical at SHA-256 `860d781998acaeeeae150d4ed58e0ce84e23bfa68941d0055f3fa5e5294eae3a`. Automated validation is not user visual approval; the later focused A13 and A15 user confirmations in Parts 178-179 supersede this historical active-item state.

## Pediatric All-Family True-Endpoint Current Update

Part 178 records the user's focused confirmation that the A13 endpoint is fixed and extends the same fresh-payload test across every Pediatric template family. All 179 cards in Filled and Blank modes at `1440 x 1000`, `768 x 1000`, and `390 x 844` produced 1,074 stable endpoint states. Every modal body retained `40px` computed bottom padding, reached its true maximum scroll position, and ended within `39.125-40.921875px`, with zero unaccepted element overflow, endpoint failures, or browser errors. The existing A13 Complications horizontal connector and A7 Medication Nursing Interventions vertical connector remain narrowly classified relationship geometry, not clipping. The product required no further edit; product and temporary payload remain byte-identical at SHA-256 `860d781998acaeeeae150d4ed58e0ce84e23bfa68941d0055f3fa5e5294eae3a`. Automated validation is not user approval. The all-family endpoint regression gate is objectively passed; Part 179 later records the completed A15 focused review.

## Next Recommended Action

Run the final Pediatric cross-surface responsive, interaction/accessibility, and print handoff before any commit or push decision.
