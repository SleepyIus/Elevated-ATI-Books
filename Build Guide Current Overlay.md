# Elevated ATI Build Guide Current Overlay

Status: synchronized repository-safe recovery mirror for private master Build Guide Parts 49-100; all 24 template families, all three shells, the final cross-book integrity gate, and the user visual-review gate are complete. The clipping automation remains paused.
Mode: DESIGN-CANON / HOLD-CONTENT.
Scope: completed user-approved template design/clipping phase; content population remains paused.
Last continuity update: 2026-07-17.

This file does not approve ATI educational content population. It mirrors the current task and recovery state without private-source material. Private master Part 49 contains the complete authoritative canon, Parts 50-78 preserve recovery repairs and clipping evidence, Part 79 records automation ownership, Parts 80-95 record the completed v4 AI-specification project and handoff, Parts 96-97 record the passed MH and AMS shell clipping audits, Part 98 records final cross-book integrity completion plus the automation pause, Part 99 records the user-approved MN Student Name writing-line alignment, and Part 100 records completion of the overall user template visual-review gate; this file must remain synchronized after every completed batch.

## Current Reading Rule

1. Follow the latest explicit user instruction first. Only the user may change the current project mode.
2. Use Part 49 and the newest later continuation in the private `Elevated_ATI_Build_Guide_v3.md` as the complete living master canon and migration reference.
3. Use this file as its repository-safe current-state and recovery mirror.
4. Use `MN Design Canon Checklist.md` as a synchronized implementation checklist and `AMS Prototype MN Canon Audit.md` as historical AMS audit evidence; neither may override private master Part 49.
5. Use the archived branch `codex-build-guide-archive-20260707` only as the immutable July 4 baseline; its older recommendations do not override this living overlay.
6. If any guide recommends content expansion while mode is `DESIGN-CANON / HOLD-CONTENT`, pause and report that content population is not active.

## Current Objective

Build the durable shell/foundation before further content work. The accepted design canon uses a clear reference hierarchy:

- MN is the primary structural, template-geometry, spacing, responsive, and mobile-layout reference.
- AMS is the polished visual reference for typography finish, controls, modal presentation, compactness, and general visual refinement.
- Preserve each book's accent color and subject identity. Use original ATI template layouts only for official structural labels and box relationships, never as permission to add content.
- When a difference is not already resolved by canon, stop for focused comparison rather than inventing a broad hybrid rule.

Completed automation objectives: private `Elevated_ATI_Build_Guide_v4_AI_Spec.md` is a self-contained, cold-start-validated AI execution specification with 1617 unique normative requirements, and the complete MN/MH/AMS template clipping matrix has passed final cross-book integrity. The user completed and approved the overall template visual review on 2026-07-17. Both automations remain paused.

## Accepted Template Canon Snapshot

- Main template landing pages should use MN's spacing rhythm, typography scale, search panel treatment, progress rows, footer/reset treatment, and measured landmark alignment. At desktop and split-pane widths, each collapsed template accordion renders at 92px outer height with a 90px header, 18px 22px header padding, 14px flex gap, and an 8px progress track. Keep the header controls as direct flex children in this order: caret, title, A-page badge, row print button, studied count, progress track. This direct order preserves MN's badge and print-control placement across pane widths.
- Opened template modals should keep the larger AMS-style title treatment while using MN's readable body text, calmer controls, source-line structure, box spacing, and mobile stacking behavior.
- Every worked template card should render its empty Student Name value as the shared AMS/MH-style writing line: at least `120px` wide, `18px` high, and a `1px dashed` current-border-color bottom edge in both Filled and Blank modes. The line must remain inside its metadata value cell at desktop, split-pane/tablet, and phone widths.
- For A1/A3/A5/A7/A9 typography, use MN A7 Medication's `Complications` box as the readable body-text canon across books: actual `p`, `li`, and direct body text in both main and nested boxes uses 16px DM Sans at 1.6 line-height with normal weight. Keep existing MN heading-strip typography unchanged. Validate rendered child text rather than only the parent `.content`, because child rules can silently restore smaller text.
- `Open chapter` links belong on their own line below the source chapter/title line.
- Internal source labels such as `ALS in book` should not appear in learner-facing source lines.
- A5 Growth and Development should preserve MN's outer section card, inner boxes, connector behavior, and progress bar. The Expected Growth and Development divider and child boxes stay inside the bordered, padded outer group; only the Health Promotion tree keeps a transparent outer wrapper for its connectors. Wide-grid siblings remain equal through normal row stretching, while each row is sized by its tallest real content; do not impose fixed `260px`/`300px` minimum heights. At split-pane and mobile widths, stacked A5 boxes use natural content height so sparse books do not retain large empty regions.
- A7 Medication should preserve the official MN/ATI medication skeleton, including Purpose of Medication, official left/right body columns, and the Nursing Interventions to Client Education connector.
- A9 Nursing Skill should preserve the official six-box skeleton and keep source sections routed into the correct boxes instead of removing empty official slots. The two nested Considerations boxes use the same 16px DM Sans, 1.6 line-height body typography as the direct A9 boxes. A9 uses book-accent bullets, muted body text, and the book's solid primary text color for bold emphasis, including `Pre:`, `Intra:`, and `Post:`. Match MN's rounded A9 corner family exactly: 16px for the Considerations group, 14px for direct boxes with 13px header caps, and 11px for nested boxes with 10px header caps. At MN's wide and split-pane sizes, preserve the two-column procedure grid; collapse to one column only at the MN phone breakpoint of 680px. A9-specific grid-area rules must agree with the active column count so no implicit 2px column or oversized Considerations row can form.
- A11 System Disorder should preserve the official skeleton. On wide screens, Safety Considerations and Complications stay in one right rail with equal content-aware rows that grow to the larger content requirement; do not use shrinkable fractional rows or forced full-height children that can clip text. At narrow/tablet widths, explicitly override A11's higher-specificity desktop tracks so both the outer grid and filled top row use one column; never combine a one-column area map with two-column tracks. The Safety rail returns to natural vertical stacking, Assessment remains a 2-by-2 inner grid, and Patient-Centered Care uses the stacked five-box layout for readability.
- A13 Therapeutic Procedure follows the shared procedure-family sizing rules. At wide and split-pane widths, paired rows stay equal but grow to the larger real content requirement; direct Considerations subboxes must not shrink or clip. At narrow/mobile widths, use plain MN-style vertical stacking with natural-height Considerations boxes.
- A15 Concept Analysis preserves the simple official skeleton: two metadata rows (`Student Name` and `Concept Analysis`), Defining Characteristics spanning both wide columns, then paired Antecedents/Negative Consequences and Related Concepts/Exemplars. Use the shared 14px box gap and corners, 16px/1.6 body typography, natural content heights, and A15's own approximately 720px one-column breakpoint.
- Low-priority controls such as `Reset All Progress` should use MN's quiet text-button treatment: transparent background, inherited font, 11px size, and normal weight.

## Rollout Rules

- Do not roll out to all books in one run.
- Work one book template file at a time.
- Within each book, work one template family or one visual defect per batch.
- Preserve each book's accent color, subject identity, template counts, and existing educational content.
- Do not add templates, flashcards, prompts, cards, sections, enrichment, or ATI educational content while content population is paused.
- Use recovered screenshots only as QA/reference context, not public content.
- Before editing, state the exact file and layout area. Validate with static checks, script parsing, and visual comparison after editing.
- Never mark a visual task approved without user confirmation.
- Never stage, commit, push, merge, reset, revert, or delete without the user's explicit instruction.

## Documentation Continuity Protocol

- After every completed batch, update the private master Build Guide and this living overlay with the active book/template, completed work, files involved, validation, visual-approval status, unresolved issue, Git state, and exactly one next action.
- Check every current guide after each batch and update every affected guide so the current documents cannot contradict one another.
- Update `MN Design Canon Checklist.md` whenever an accepted canon rule is added, changed, clarified, or rejected.
- Update `AMS Prototype MN Canon Audit.md` whenever AMS audit status, comparison evidence, or rollout readiness changes.
- Keep the archived July 4 branch immutable as the recovery baseline.
- No migration-critical decision, pending approval, active task, or next action may exist only in chat history.
- Before ending a batch, confirm that a new task could resume safely from the private master, this overlay, the canon checklist, and the Git diff.

## Current Rollout Status

AMS and MN Templates have completed their major design-canon alignment. Their accepted comparison work established:

- main template landing-page alignment;
- source-line and modal header rhythm;
- quiet text/control token matching;
- A5 Growth and Development mobile/card behavior;
- A7 Medication structure and connector behavior;
- A9 Nursing Skill official-box preservation;
- A11 System Disorder Assessment and stacked Patient-Centered Care behavior;
- A13 procedure-family narrow/mobile behavior;
- A15 visual-token consistency.

MN has been changed only when explicitly approved during this prototype work, such as adding Previous/Next navigation to MN template modals, applying the stacked five-box Patient-Centered Care narrow layout, and adding the shared AMS/MH-style dashed Student Name writing line to every MN worked template card.

The MH Templates focused rollout is complete. The landing shell, sidebar controls, typography, accordion geometry, A-page badges, print controls, A3/A5/A7/A9 structures, and A9 corner/emphasis canon were aligned in small visual batches. MH A11 System Disorder now explicitly stacks the outer grid, filled top row, Safety rail, and five Patient-Centered Care boxes at narrow/tablet widths while preserving the 2-by-2 Assessment grid. MH A13 Therapeutic Procedure now replaces its higher-specificity desktop tracks with one full-width column and natural-height Considerations boxes at narrow/tablet widths. MH A15 Concept Analysis now uses MN's shared visual tokens, natural heights, official two-row metadata, and two-column-to-one-column responsive map. The final shell pass added MN's `max-width:1100px` main-workspace padding to MH and objectively validated the landing shell, sidebar, accordion, modal, navigation, count, clipping, search, and responsive behavior.

The MN A13 repair reported after that queue now passes objective clipping QA. Its content-aware procedure rows preserve equal left/right sizing while allowing the taller real content to determine row height. The user completed and approved the overall template visual review on 2026-07-17.

## Current Recovery Snapshot

- Completed documentation files: private v4, private v3 continuity ledger, this overlay, the implementation checklist, and the AMS evidence ledger. No clipping matrix item remains active.
- Final-integrity baseline: the MN, MH, and AMS clipping matrix passed before the later user-approved MN Student Name writing-line alignment. The current MN payload now has SHA-256 `5d59aeaf4dc21236d989671a725a5803073c9a96e5e32f9842af79540ac0b31e`; four scripts, 197 cards, 197 unique IDs, family order, counts, and shell landmarks remain unchanged, and both comparison pages embed the exact current MN payload.
- Heartbeat status: `ATI Build Guide AI Spec Polish` (`ati-build-guide-ai-spec-polish`) is paused. `ATI Template Clipping QA` (`ati-template-clipping-qa`) is paused after final integrity, with its protected configuration unchanged.
- Approved comparison previews: `http://127.0.0.1:8765/elevated-ati-template-compare-mh-reference.html` and `http://127.0.0.1:8765/elevated-ati-template-compare-ams-mn.html`; both returned HTTP 200 and the user approved their current visual direction on 2026-07-17.
- User visual-review gate: **COMPLETE / APPROVED**. The user reviewed both comparison pages and accepted the current MN/MH/AMS template direction, including the MN Student Name writing line, on 2026-07-17. This subjective approval follows and remains distinct from automated QA.
- MN Student Name writing-line alignment: complete. One focused CSS selector now gives all 197 unchanged empty Student Name values the shared `120px` by `18px`, `1px dashed` line in Filled and Blank modes. Automated rendered measurements at `1440 x 1000`, `768 x 1000`, and `390 x 844` found the line fully contained with `0px` page and modal horizontal overflow. No learner-facing text, labels, source lines, counts, IDs, card order, or educational content changed.
- First automated batch status: complete. At a 1440px comparison viewport with approximately 720px panes, automated rendered QA found that Filled-mode Patient-Centered Care still inherited the desktop three-column tracks. The focused selector-specificity repair now gives all five boxes the full 548px content width; the 570px outer grid and top row are one column, Assessment remains 269px/269px, Safety stacks naturally, and no A11 box overflows.
- Second automated batch status: complete. At approximately 640px comparison panes, automated rendered QA found that MH A13 retained its desktop two-column area map beside a one-column narrow rule, collapsing one track to 2px and stretching the modal to 10,875px. The focused Therapeutic Procedure override now renders one 490px-wide track in the required six-region order, uses natural-height Considerations subboxes, has no box overflow, and reduces the modal to 1,733px. Scripts parse, the embedded comparison payload matches the current 71-card MH file, and all 10 A13 entries remain.
- Third automated batch status: complete. At approximately 1100px and 640px comparison panes, MH A15 now matches MN's 14px gap and corners, 16px/1.6 body typography, official two-row metadata, natural content heights, and two-column-to-one-column responsive behavior. All five narrow regions render at 490px with no box overflow. Scripts parse, the comparison payload matches, and all 71 cards, 71 unique IDs, and 6 A15 entries remain.
- Fourth automated batch status: complete. MH now mirrors MN's `max-width:1100px` `.template-main` padding of `40px 32px 80px`, removing the former 8px split-pane landmark offset. At approximately 1100px panes, the hero, title, body, divider, search panel, and first accordion share the same vertical landmarks and content left edge. Narrow panes preserve the accepted off-canvas sidebar and mobile padding behavior. Mobile modal, same-type Previous/Next, source link, print and mode controls, search synchronization, and count restoration were exercised without horizontal overflow. Static checks confirmed 71 cards, 71 unique IDs, eight groups, expected per-type counts, exact comparison payload parity, script parsing, HTTP 200, and a clean diff check.
- MN A13 clipping batch status: objectively passed with no additional template edit. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 22 cards passed in Filled and Blank modes, all 132 state measurements were stable on repeat, and the collapsed header passed at every width. No confirmed clipping remained. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 22 A13 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A1 clipping batch status: objectively passed after the focused Bishop Score repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 46 cards passed in Filled and Blank modes, all 276 state measurements were stable on repeat, and the collapsed header passed at every width. The Bishop Score table remained clear, with no additional confirmed defect. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 46 A1 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A3 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 15 cards passed in Filled and Blank modes, all 90 state measurements were stable on repeat, and the collapsed header passed at every width. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 15 A3 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A5 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 7 cards passed in Filled and Blank modes, all 42 state measurements were stable on repeat, and the collapsed header passed at every width. A5 outer groups, child boxes, and Health Promotion relationship geometry remained within their intended containers. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 7 A5 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A7 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 18 cards passed in Filled and Blank modes, all 108 state measurements were stable on repeat, and the collapsed header passed at every width. A7 Purpose, medication columns, direct and nested boxes, controls, and the Nursing Interventions-to-Client Education relationship stayed within their intended containers. A 7px list-row scroll delta at split-pane width was verified as visible list-marker geometry fully contained within its parent, not clipping. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 18 A7 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A9 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 15 cards passed in Filled and Blank modes, all 90 state measurements were stable on repeat, and the collapsed header passed at every width. The six official regions, seven measured direct/nested boxes, Considerations geometry, controls, and content remained within their intended containers. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 15 A9 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A11 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 65 cards passed in Filled and Blank modes, all 390 state measurements were stable on repeat, and the collapsed header passed at every width. The top row, Assessment, Safety, Complications, Patient-Centered Care, controls, and content remained within their intended containers. An initial bounds signal was rejected after confirming that `.sd-middle` intentionally uses `display: contents`; corrected measurements against `.sd-grid` were clear. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 65 A11 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN A15 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 9 cards passed in Filled and Blank modes, all 54 state measurements were stable on repeat, and the collapsed header passed at every width. The five official boxes, two metadata rows, controls, and content remained within their intended containers, with no horizontal page overflow. Static validation confirms four MN scripts parse, 197 cards and 197 unique IDs remain, all 9 A15 cards remain, and the comparison embeds the exact current MN payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A1 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 13 cards passed in Filled and Blank modes, all 78 state measurements were stable on repeat, and the collapsed header passed at every width. The three official Basic Concept boxes, three metadata rows, controls, and content remained within their intended containers, with no horizontal page overflow. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 13 A1 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A3 clipping batch status: objectively passed after two focused repairs. The first repair replaced the conflicting narrow area map and collapsed `2px 606px` tracks with one full-width column and natural-height boxes. The second repair follows MN relationship canon by hiding the horizontal Potential Complications connector only when MH A3 stacks at `max-width:860px`. All six cards passed in Filled and Blank modes at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 36 state measurements were stable on repeat, and the collapsed header passed at every width. Desktop retains the visible contained connector and `502px 502px` columns; tablet and phone retain full-width one-column boxes with no obsolete horizontal bridge. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A3 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A5 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all six cards passed in Filled and Blank modes, all 36 state measurements were stable on repeat, and the collapsed header passed at every width. Both official outer groups, all eight child boxes, three metadata rows, controls, direct content, and Health Promotion relationship geometry remained within their intended containers. Desktop preserved its contained four-column connector tree; tablet and phone used natural-height one-column stacks with the obsolete wide connector hidden. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A5 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A7 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 12 cards passed in Filled and Blank modes, all 72 state measurements were stable on repeat, and the collapsed header passed at every width. Both Purpose boxes, four official left-column boxes, three official right-column boxes, four metadata rows, controls, content, and the Nursing Interventions-to-Client Education connector remained within their intended containers. Desktop preserved the two-column medication body; tablet and phone used the intended one-column body while the connector stayed visible and contained. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 12 A7 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A9 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all six cards passed in Filled and Blank modes, all 36 state measurements were stable on repeat, and the collapsed header passed at every width. The six official regions, five direct boxes, two nested Considerations boxes, three metadata rows, controls, and content remained within their intended containers. Desktop retained its contained two-column connector geometry, split-pane/tablet retained two columns without clipping, and phone used one full-width column. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A9 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A11 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 12 cards passed in Filled and Blank modes, all 72 state measurements were stable on repeat, and the collapsed header passed at every width. The three-box top row, four-box Assessment group, Safety/Complications rail, five Patient-Centered Care boxes, three metadata rows, controls, and content remained within their intended containers. Desktop preserved the established wide geometry; split-pane/tablet and phone used one-column outer/top tracks with natural Safety stacking while retaining the 2-by-2 Assessment grid. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 12 A11 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A13 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 10 cards passed in Filled and Blank modes, all 60 state measurements were stable on repeat, and the collapsed header passed at every width. The six official regions, five direct boxes, Considerations group with two nested subboxes, three metadata rows, controls, content, and relationship connector remained within their intended containers. Desktop retained the contained `502px 502px` two-column grid and connector; split-pane/tablet and phone used natural-height one-column tracks with the obsolete horizontal bridge hidden. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all 10 A13 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MH A15 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all six cards passed in Filled and Blank modes, all 36 opened-card states plus three collapsed-header states were stable on repeat. The five official boxes and two metadata rows remained within their intended containers. Desktop retained the `500px 500px` two-column grid; split-pane/tablet used one `618px` track and phone used one `354px` track. No page, box, control, or sibling overflow was confirmed. Static validation confirms one MH script parses, 71 cards and 71 unique IDs remain, all six A15 cards remain, and the comparison embeds the exact current MH payload. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A1 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 21 visible A1 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 126 opened-card states plus three collapsed-header states were stable on repeat. Every opened state retained three Basic Concept boxes and three metadata rows. Desktop retained three equal `328.664px` columns; split-pane/tablet used one `732px` track and phone used one `354px` track. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 21 A1 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A3 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all eight visible A3 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 48 opened-card states plus three collapsed-header states were stable on repeat. Every opened state retained seven direct/nested boxes and three metadata rows. Desktop retained the `500px 500px` procedure grid; split-pane/tablet used one `732px` track and phone used one `354px` track. The repeated 14px desktop `scrollWidth` delta on Potential Complications was verified as its intentional visible `::after` connector exactly spanning the 14px grid gap; the box content itself had zero overflow and the connector remained inside the grid, so it was not clipping. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all eight A3 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A5 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; its one visible prototype fixture passed in Filled and Blank modes, all six opened-card state measurements were stable on repeat, and the collapsed header passed at every width. Every opened state retained eight official boxes, two groups, the Health Promotion parent/connector relationship, and three metadata rows. Desktop retained four columns in both child rows, split-pane/tablet retained two columns, and phone used one full-width column with the connector hidden by the established mobile rule. The visible desktop/tablet connector overflow was verified as intentional stems that remain inside the Health Promotion group and do not clip content or sibling boxes. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, the one A5 prototype fixture remains, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A7 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 16 visible A7 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 96 opened-card states plus three collapsed-header states were stable on repeat. Every opened state retained nine official boxes, including both Purpose boxes, four left-column boxes, three right-column boxes, and four metadata rows. Desktop retained the two-column medication body; split-pane/tablet and phone used full-width stacked medication columns. The repeated 24px vertical delta on Nursing Interventions was verified as its intentional visible connector exactly crossing the gap to Client Education while remaining inside the right medication column; neither box's content overflowed. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 16 A7 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A9 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all four visible A9 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 24 opened-card state configurations plus three collapsed-header states were stable on repeat. Every opened state retained seven official direct/nested boxes, the Considerations parent, and three metadata rows. Desktop retained the `500px 500px` two-column procedure grid; split-pane/tablet and phone used full-width one-column grids in the accepted narrow order. The repeated 14px desktop delta on Potential Complications was verified as its intentional visible connector spanning the procedure-grid gap toward Nursing Interventions while remaining inside the grid; the connector is hidden by the established narrow rule. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all four A9 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A11 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 16 visible A11 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 96 opened-card state configurations plus three collapsed-header states were stable on repeat. Every opened state retained the three-box top row, four-box Assessment group, Safety Considerations plus Complications rail, five-box Patient-Centered Care group, and three metadata rows. Desktop retained the two-column outer grid, three-column top row, two-column Assessment, subgrid Safety rail, and three-column Patient-Centered Care geometry. Split-pane/tablet and phone used one-column outer/top tracks, retained two-column Assessment, stacked Safety/Complications naturally, and adapted Patient-Centered Care between one and two contained tracks according to the existing visible-content class. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 16 A11 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A13 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; all 17 visible A13 cards, including the existing prototype fixture, passed in Filled and Blank modes. All 102 opened-card state configurations plus three collapsed-header states were stable on repeat. Every opened state retained seven official direct/nested boxes, the Considerations parent, and three metadata rows. Desktop retained the `500px 500px` two-column procedure grid; split-pane/tablet and phone used full-width one-column grids in the accepted narrow order. The repeated 14px desktop delta on Potential Complications was verified as its intentional visible connector spanning the procedure-grid gap toward Nursing Interventions while remaining inside the grid; the connector is hidden by the established narrow rule. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, all 17 A13 cards remain, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- AMS A15 clipping batch status: objectively passed with no template repair. The exact current standalone payload was checked at `1440 x 1000`, `768 x 1000`, and `390 x 844`; its one visible prototype fixture passed in Filled and Blank modes, all six opened-card state configurations were stable on repeat, and the collapsed header passed at every width. Every opened state retained five official Concept Analysis boxes and the three current rendered metadata rows. Desktop retained the `500px 500px` area map, split-pane/tablet retained two `359px` columns, and phone used one full-width `354px` column. Paired rows remained equal at wide widths, all boxes used natural contained heights, and no page, control, content, or sibling overflow was confirmed. Static validation confirms the single AMS script parses, 84 cards and 84 unique IDs remain, the one A15 prototype fixture remains, and both the standalone preview and AMS/MN comparison embed the exact current AMS payload with SHA-256 `7f995e9c84552880bdfc103083f12c6c4fe90da9dd4e1a27c23ebe9607742998`. This clipping-only pass did not change source-line metadata or establish a new metadata-row canon decision. User visual approval remained pending at this automated-batch checkpoint; the overall template visual review was completed on 2026-07-17.
- MN shell clipping batch status: objectively passed with no template repair. The exact current standalone payload, local preview, and rebuilt temporary comparison matched with SHA-256 `ca58dc999c302ed8e20969d56bfb690bcad1a8080699056752becc05973b5900`; four scripts parsed, 197 cards and 197 unique IDs remained, eight family accordions stayed in canonical order, and 11 required shell landmarks remained. At `1440 x 1000`, `820 x 1000`, and `390 x 844`, all eight collapsed headers, landing controls, fixed/sidebar-drawer states, and one representative Basic Concept modal chrome state were stable on repeat. Page and modal-body horizontal overflow remained `0px`. The responsive drawer remained fully in view with `aria-expanded="true"`; intentional closed off-canvas and vertical modal-body scrolling were excluded. The preview was restored to a clean default landing state. Automated QA is not user visual approval.
- MH shell clipping batch status: objectively passed with no template repair. The current repository and standalone preview matched at SHA-256 `21fb16085668db5fa8d432e2bd8a20d149db947b2650c4d3ae53465f114c9d27`; the comparison embedded the exact payload; the single script parsed; and 71 cards, 71 unique IDs, eight groups, and counts `13/6/6/12/6/12/10/6` remained. At `1440 x 1000`, `820 x 1000`, and `390 x 844`, eight landing/modal/sidebar states were identical on repeat at least 300ms apart. Page, collapsed-header, open-sidebar, modal-control, and modal-body horizontal overflow remained `0px`; all eight collapsed headers and all eight open-drawer sidebar rows passed. Tablet/phone modal-body vertical scrolling was intentional, closed off-canvas state was excluded, and visible-overflow heading line-box deltas had no truncated edge. Browser console errors were zero. No screenshot or product repair was permitted. Automated QA is not user visual approval.
- Availability note: the user also observed one selected-model capacity warning. It may skip or delay an automation run but does not change project files; switch models only if it persists.
- Build Guide polishing Batches 1-16 are passed. Private v4 contains the complete AI execution specification and begins with the exact marker `ELEVATED_ATI_AI_SPEC_STATUS: COMPLETE_AND_COLD_START_VALIDATED`. Post-completion canon synchronization now reports 1617 unique requirements, complete section/traceability/protocol coverage, and matching cold-start artifacts. Automated validation is not user approval.
- Hub evidence recorded in Batch 5: all five current Hub shells have four ordered counters, three Primary tools, the manifest unit count, eight Template type rows, unique IDs, and parseable scripts. AMS retains the measured `84/76` Templates and `208/0` Starter Cards payload/display mismatches. PD has eight and Pharm has seven Hub Template-type hash targets that do not resolve as literal or script-handled family anchors in their current Templates files. These are documented `OBSERVED_MISMATCH` facts only; no Hub or Templates repair is authorized during Build Guide polishing.
- Book evidence recorded in Batch 6: current chapter/planner counts are AMS `96/96`, MH `33/33`, MN `27/27`, PD `44/44`, and Pharm `49/49`; all five files retain unique static IDs, required modal landmarks, four parseable inline scripts, intentional modal-body scrolling, and phone modal rules. MN lacks page-load/hashchange chapter routing, PD `ch35` and `ch37` lack Template bridges, and Pharm has TL;DR cards in 12 of 49 chapters and Template bridges in none. These are documented `OBSERVED_MISMATCH` facts only; no Book repair is authorized during Build Guide polishing.
- Templates evidence recorded in Batch 7: all five current Templates files retain the fixed eight-family A1-A15 registry, unique card/static IDs, parseable scripts, resolving local paths, Filled/Blank state, search, progress, same-family modal navigation, print, and modal controls. Family counts are AMS `21/8/1/16/4/16/17/1`, MH `13/6/6/12/6/12/10/6`, MN `46/15/7/18/15/65/22/9`, PD `12/9/5/13/9/116/9/6`, and Pharm `1/3/0/141/18/35/9/42`. AMS retains 76 learner/source-linked cards plus eight hidden prototype fixtures, Pharm A5 retains its zero-card placeholder, and AMS A15 currently renders three metadata rows against the accepted two-row canon. These are evidence or `OBSERVED_MISMATCH` facts only; no Templates repair is authorized during Build Guide polishing.
- Flashcards evidence recorded in Batch 8: current starter-card payloads are AMS/MH/MN/PD/Pharm `208/593/492/747/702`; all card IDs are unique, scripts parse, and every current source anchor resolves to the same-subject Book. AMS remains the older flat shell and displays seven types while its cards use five distinct values. The four mature shells split `easeFactor`/`repetitions` initialization from `ef`/`reps` rating aliases, and their Browse path can discard a selected non-due card. These remain `OBSERVED_MISMATCH` facts only; no Flashcards repair or migration is authorized during Build Guide polishing. Batch 8 verified 196 sequential `FC` requirements and 843 total unique v4 declaration IDs; the earlier 655 Batch 7 total was corrected to 646 without changing a requirement.
- Placeholder/new-husk protocol recorded in Batch 9: existing subjects must be measured and preserved rather than replaced; new subjects require explicit identity, edition, accent, four paths, isolated storage, and collision-free plans. Donor payload and identity must be removed outside the repository before a destination write. Placeholder construction order is Book, Templates, Flashcards, then Hub, one separately authorized surface per batch. Exact empty states and readiness flags keep content population paused. Batch 9 verified 120 sequential `HUSK` requirements and 964 total unique v4 declaration IDs; no repository husk or product surface was created or changed.
- Empty-box/content-population protocol recorded in Batch 10: only a current explicit user instruction can activate one bounded content batch for one subject and one surface. A private provenance map must cover 100% of the focused learner-facing diff with exact same-subject source locations and destination routes; inference, model memory, web material, donor content, and broad topic similarity cannot close a source gap. Required official boxes remain visible and machine-flagged when unsupported instead of being deleted or filled with stubs. Default caps are two Book chapters, three Template cards, or twelve Flashcards, with Hub count/status synchronization handled as a separate surface batch. Authorization expires when the batch closes, blocks, or reaches its cap, returning the project to HOLD-CONTENT. Batch 10 verified 135 sequential `CONTENT` requirements and 1100 total unique v4 declaration IDs; no ATI source, repository product surface, learner-facing content, count, link, card order, or manifest changed.
- Deterministic build pipeline recorded in Batch 11: every work order selects one focused-repair, placeholder-suite, source-authorized-content, or documentation lane and owns one subject, one surface, one primary file, and one bounded objective. New/recreated placeholder suites proceed as validated manifest, Book, Templates, Flashcards, then Hub; existing repairs begin directly with the named surface. The fixed phase order is authority/mode, work order, preflight, baseline, temporary preparation, implementation, static validation, rendered/behavior validation, focused diff, continuity, approval gate, and handoff. Product and documentation ownership are explicit, objective QA remains distinct from user approval, and no passed batch authorizes Git publication or another lane. Batch 11 verified 140 sequential `BUILD` requirements and 1241 total unique v4 declaration IDs; no product surface, learner content, source artifact, automation configuration, or AMS audit changed.
- QA specification recorded in Batch 12: every batch uses a requirement-backed matrix whose required rows must all be `PASS` or justified `NOT_APPLICABLE`, with zero `FAIL` or `BLOCKED` rows. Static QA uses structured parsing and complete scripts/targets; payload QA uses complete sets and independently measured counts; navigation requires zero unresolved live or unauthorized cross-subject targets; storage uses an isolated context; rendered QA uses `1440 x 1000`, `768 x 1000`, and `390 x 844` plus exact breakpoints; clipping requires more than 1px objective overflow and two identical readings at least 250ms apart. Screenshots remain supplemental under `/private/tmp`, and focused success never implies full conformance or user approval. Batch 12 verified 160 sequential `QA` requirements and 1402 total unique v4 declaration IDs; no product surface, source artifact, automation configuration, or AMS audit changed.
- Recovery/operations specification recorded in Batch 13: each active batch has one durable work order containing authority, mode/lane/scope, exact file/targets, editable/supporting/forbidden files, manifest/payload baseline, QA/approval gates, Git state, automation preservation, status, and one next action. Recovery uses current files/configurations only, resumes from the first unpassed phase, classifies blockers explicitly, keeps v3 append-only and v4 IDs stable, and treats commit and push as separately authorized operations. The final automation transition remains reserved for Batch 16 and may change only status fields after exact configuration capture/verification. Batch 13 verified 140 sequential `RECOVERY` requirements and 1543 total unique v4 declaration IDs; no product surface, source artifact, Git state, automation configuration, or AMS audit changed.
- Cold-start validation recorded in Batch 15: v4 alone produced a temporary test-only manifest, Book-to-Templates-to-Flashcards-to-Hub build plan, and validation report under `/private/tmp`. The initial synthetic code was rejected on a real repository identity collision; the accepted `V4X` fixture had zero path, identity, slug, course-label, namespace, or key matches. The final matrix reported 14 PASS, 0 FAIL, and 0 BLOCKED; all learner payloads remained empty, no product file was created or edited, and automated validation did not grant user approval or real-subject authority. Batch 15 verified 20 sequential `COLD` requirements and 1581 total unique v4 declaration IDs.
- Clipping matrix status: complete. All 24 MN, MH, and AMS template-family items, the MN/MH/AMS shells, and the final cross-book integrity gate are objectively passed and must not be repeated without a concrete regression.
- `ati-template-clipping-qa` is configuration-identical and PAUSED after automated QA and user visual review both completed.
- Current tracked modifications: `Build Guide Current Overlay.md`, `MN Design Canon Checklist.md`, `AMS Prototype MN Canon Audit.md`, `Elevated ATI AMS Templates.html`, `Elevated ATI MN Templates.html`, and `Elevated ATI MH Templates.html`.
- Current branch: `main`, one commit ahead of `origin/main`; existing unpushed commit `33d0968 Fix AMS template hero button alignment`.
- Known local untracked files: `.DS_Store`, `Teleprompter.html`, and `recovered-codex-screenshots/`; report them only under GitHub Desktop status.

## Next Recommended Action

Wait for the user's explicit selection of the next project lane.
